---
phase: 03-core-ui
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-03, 03-04]
files_modified: []
autonomous: false
requirements: [GRID-01, GRID-02, GRID-03, GRID-04, GRID-05, GRID-06, GRID-07, DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "All 12 hospitals appear in the grid with correct data"
    - "Clicking a hospital opens the Power BI dashboard"
    - "Dark mode toggle works and persists"
    - "User avatar and email visible in header"
    - "Search filters hospitals correctly"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete Phase 3 UI. Human verifies that all components render correctly, dark mode works, search filters, Power BI loads, and the overall experience matches expectations.

Purpose: Phase 3 delivers the entire user-facing product. A human checkpoint ensures the visual design, interactions, and data display meet quality expectations before moving to Phase 4 (i18n).
Output: Human approval or list of issues to fix
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and start the application for verification</name>
  <files></files>
  <action>
1. Ensure Docker Compose is running with the latest migrations:
   ```bash
   docker compose down && docker compose up -d --build
   ```
2. Wait for services to be healthy
3. Verify the migration 004 ran (check docker logs for the API container)
4. Open browser check: curl http://localhost:3000 returns 200
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000</automated>
  </verify>
  <done>Application running with all Phase 3 changes applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 3 Core UI: hospital card grid with search/filter/pagination, Power BI dashboard embedding with loading/error/fullscreen, global header with user profile and dark mode toggle, global footer.</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000 -- should redirect to login
    2. Log in with Google OAuth -- should see hospital grid
    3. Verify header: GIS logo/name on left, dark mode toggle + user avatar on right
    4. Click user avatar -- should show dropdown with name, email, logout
    5. Toggle dark mode -- colors should switch, reload page -- preference persists
    6. Verify grid: 12 hospital cards with logos (8 with images, 4 with fallback icons), names, CNES codes, and date periods
    7. Type in search box -- cards should filter by name or CNES instantly
    8. Check pagination if visible (with 12 hospitals and page size 9, should show 2 pages)
    9. Click a hospital card -- should navigate to /hospital/{cnes}
    10. Dashboard page: hospital name displayed, back button visible, Power BI iframe loading
    11. Wait for iframe to load (3-10 seconds) -- loading spinner should disappear
    12. Click fullscreen toggle -- iframe should go fullscreen, press Escape to exit
    13. Verify footer: company branding visible on both grid and dashboard pages
    14. Check mobile responsiveness: resize browser to narrow -- grid should be 1 column
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
Human visual verification of all Phase 3 success criteria.
</verification>

<success_criteria>
- Human approves the complete Phase 3 UI experience
- All 12 hospitals display correctly with real data
- Power BI dashboards load in iframes
- Dark mode, search, pagination, fullscreen all functional
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-ui/03-05-SUMMARY.md`
</output>
