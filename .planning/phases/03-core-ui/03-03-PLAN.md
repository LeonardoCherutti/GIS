---
phase: 03-core-ui
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - frontend/src/components/hospital/HospitalCard.tsx
  - frontend/src/components/hospital/HospitalCardSkeleton.tsx
  - frontend/src/components/hospital/HospitalGrid.tsx
  - frontend/src/components/hospital/SearchInput.tsx
  - frontend/src/components/hospital/Pagination.tsx
  - frontend/src/app/hospital/page.tsx
autonomous: true
requirements: [GRID-01, GRID-02, GRID-03, GRID-04, GRID-05, GRID-06, GRID-07]

must_haves:
  truths:
    - "Hospital cards show logo, name, CNES code, and data period"
    - "Grid is responsive: 1 col mobile, 2 col tablet, 3 col desktop"
    - "Clicking a card navigates to /hospital/{cnes}"
    - "Search input filters hospitals by name or CNES code instantly"
    - "Skeleton loading placeholders appear while data loads"
    - "Pagination controls appear below the grid"
  artifacts:
    - path: "frontend/src/components/hospital/HospitalCard.tsx"
      provides: "Reusable hospital card with logo, name, CNES, period"
      exports: ["HospitalCard"]
    - path: "frontend/src/components/hospital/HospitalCardSkeleton.tsx"
      provides: "Skeleton loading placeholder matching card dimensions"
      exports: ["HospitalCardSkeleton"]
    - path: "frontend/src/components/hospital/HospitalGrid.tsx"
      provides: "Grid orchestrator with search, pagination, skeleton loading"
      exports: ["HospitalGrid"]
    - path: "frontend/src/components/hospital/SearchInput.tsx"
      provides: "Search/filter input for hospital name or CNES"
      exports: ["SearchInput"]
    - path: "frontend/src/components/hospital/Pagination.tsx"
      provides: "Pagination controls"
      exports: ["Pagination"]
    - path: "frontend/src/app/hospital/page.tsx"
      provides: "Hospital grid page using HospitalGrid component"
  key_links:
    - from: "frontend/src/components/hospital/HospitalGrid.tsx"
      to: "frontend/src/lib/api/client.ts"
      via: "apiFetch('/hospitals')"
      pattern: "apiFetch.*hospitals"
    - from: "frontend/src/components/hospital/HospitalCard.tsx"
      to: "/hospital/[cnes]"
      via: "Link href"
      pattern: "href=.*hospital/"
    - from: "frontend/src/app/hospital/page.tsx"
      to: "frontend/src/components/hospital/HospitalGrid.tsx"
      via: "import HospitalGrid"
      pattern: "import.*HospitalGrid"
---

<objective>
Build the hospital card grid with reusable card components, search/filter, skeleton loading, and pagination.

Purpose: Replace the basic link-list grid from Phase 2 with polished hospital cards showing logo, name, CNES, and period. Add search filtering and pagination for the grid. This is the primary browsing interface.
Output: 5 new hospital components + refactored grid page
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-ui/03-RESEARCH.md

@frontend/src/app/hospital/page.tsx (current grid page -- will be refactored by Plan 02)
@frontend/src/components/ui/card.tsx (shadcn Card, CardHeader, CardTitle, CardContent)
@frontend/src/components/ui/skeleton.tsx (shadcn Skeleton)
@frontend/src/components/ui/input.tsx (shadcn Input -- installed in Plan 02)
@frontend/src/lib/api/client.ts (apiFetch function)
@index.html (original card design reference: glass-box, logo circle, name, CNES, period)

<interfaces>
From frontend/src/lib/api/client.ts:
```typescript
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<ApiResult<T>>
// ApiResult: { ok: true, data: T } | { ok: false, error: { message: string } }
```

Hospital type (from current page.tsx):
```typescript
interface Hospital {
  id: string
  name: string
  cnes: string
  logo_url: string | null
  period_start: string | null
  period_end: string | null
  powerbi_url: string | null
  sort_order: number
  created_at: string
}
```

From frontend/src/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HospitalCard, HospitalCardSkeleton, SearchInput, and Pagination components</name>
  <files>
    frontend/src/components/hospital/HospitalCard.tsx,
    frontend/src/components/hospital/HospitalCardSkeleton.tsx,
    frontend/src/components/hospital/SearchInput.tsx,
    frontend/src/components/hospital/Pagination.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/card.tsx (Card component API)
    - frontend/src/components/ui/skeleton.tsx (Skeleton component API)
    - frontend/src/components/ui/input.tsx (Input component -- should exist after Plan 02 installs it)
    - frontend/src/components/ui/button.tsx (Button component API)
    - index.html (lines 61-220 for original card design)
    - Read node_modules/next/dist/docs/ in frontend/ for Image component API changes
  </read_first>
  <action>
1. Create `frontend/src/components/hospital/HospitalCard.tsx`:
   - Accept props: `hospital: { name, cnes, logo_url, period_start, period_end }`
   - Use shadcn Card with hover effect: `transition-all hover:-translate-y-0.5 hover:shadow-md`
   - Add left border accent: `border-l-4` with `style={{ borderLeftColor: 'var(--palette-primary)' }}`
   - Layout matching original index.html design:
     - Row with logo circle (w-12 h-12 rounded-full) + hospital name
     - Logo: if logo_url exists, use Next.js `<Image src={hospital.logo_url} alt="" width={32} height={32} />` inside a rounded container with bg-primary-subtle. If no logo_url, show Building2 icon from lucide-react
     - Hospital name as CardTitle
     - Below: CNES code and period range in muted text (text-sm text-muted-fg)
     - Period format: "jan/2021 - abr/2025" (format the ISO dates to pt-BR month/year)
   - Wrap entire card in `<Link href={/hospital/${hospital.cnes}}>` for navigation (per GRID-04)
   - Create a helper function `formatPeriod(dateStr: string | null): string` that converts "2021-01-01" to "jan/2021"
   - Use Portuguese month abbreviations: jan, fev, mar, abr, mai, jun, jul, ago, set, out, nov, dez

2. Create `frontend/src/components/hospital/HospitalCardSkeleton.tsx`:
   - Match the dimensions of HospitalCard
   - Use shadcn Skeleton component for: logo circle (w-12 h-12 rounded-full), name line (h-5 w-3/4), CNES line (h-4 w-1/2), period line (h-4 w-2/3)
   - Wrap in Card with same padding as HospitalCard
   - Export as named export

3. Create `frontend/src/components/hospital/SearchInput.tsx`:
   - Controlled input component with props: `value: string, onChange: (value: string) => void, placeholder?: string`
   - Use shadcn Input component
   - Add Search icon from lucide-react on the left side (inside a relative container with absolute positioned icon)
   - Default placeholder: "Buscar por nome ou CNES..."
   - Full width with max-w-md

4. Create `frontend/src/components/hospital/Pagination.tsx`:
   - Props: `currentPage: number, totalPages: number, onPageChange: (page: number) => void`
   - Render: Previous button, page numbers (show at most 5 page buttons with ellipsis), Next button
   - Use shadcn Button variant="outline" for page buttons, variant="default" for active page
   - Disable Previous on page 1, disable Next on last page
   - Use ChevronLeft and ChevronRight icons from lucide-react
   - Page size default: 9 hospitals per page (3x3 grid full)
   - If totalPages <= 1, render nothing (hide pagination)
  </action>
  <verify>
    <automated>ls frontend/src/components/hospital/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/components/hospital/HospitalCard.tsx
    - test -f frontend/src/components/hospital/HospitalCardSkeleton.tsx
    - test -f frontend/src/components/hospital/SearchInput.tsx
    - test -f frontend/src/components/hospital/Pagination.tsx
    - grep "logo_url" frontend/src/components/hospital/HospitalCard.tsx
    - grep "Link" frontend/src/components/hospital/HospitalCard.tsx
    - grep "Skeleton" frontend/src/components/hospital/HospitalCardSkeleton.tsx
    - grep "Search" frontend/src/components/hospital/SearchInput.tsx
    - grep "onPageChange" frontend/src/components/hospital/Pagination.tsx
  </acceptance_criteria>
  <done>4 hospital components created: HospitalCard with logo/name/CNES/period, HospitalCardSkeleton matching card dimensions, SearchInput with search icon, Pagination with page controls.</done>
</task>

<task type="auto">
  <name>Task 2: Create HospitalGrid orchestrator and refactor hospital page</name>
  <files>
    frontend/src/components/hospital/HospitalGrid.tsx,
    frontend/src/app/hospital/page.tsx
  </files>
  <read_first>
    - frontend/src/app/hospital/page.tsx (current implementation after Plan 02 refactor)
    - frontend/src/components/hospital/HospitalCard.tsx (just created)
    - frontend/src/components/hospital/HospitalCardSkeleton.tsx (just created)
    - frontend/src/components/hospital/SearchInput.tsx (just created)
    - frontend/src/components/hospital/Pagination.tsx (just created)
    - frontend/src/lib/api/client.ts (apiFetch)
  </read_first>
  <action>
1. Create `frontend/src/components/hospital/HospitalGrid.tsx`:
   - 'use client' component
   - State: hospitals (Hospital[]), loading (boolean), query (string), currentPage (number, default 1)
   - Fetch hospitals from API using apiFetch('/hospitals') in useEffect
   - Show error toast on fetch failure (import toast from sonner)
   - Implement client-side filtering with useMemo:
     ```typescript
     const filtered = useMemo(() => {
       if (!query.trim()) return hospitals
       const q = query.toLowerCase()
       return hospitals.filter(h => h.name.toLowerCase().includes(q) || h.cnes.includes(q))
     }, [hospitals, query])
     ```
   - Reset currentPage to 1 when query changes (useEffect on query)
   - Implement client-side pagination with useMemo:
     ```typescript
     const PAGE_SIZE = 9
     const totalPages = Math.ceil(filtered.length / PAGE_SIZE)
     const paginated = filtered.slice((currentPage - 1) * PAGE_SIZE, currentPage * PAGE_SIZE)
     ```
   - Render structure:
     a. SearchInput at top (full width, mb-6)
     b. Loading state: grid of 6 HospitalCardSkeleton components
     c. Empty state (after filter): "Nenhum hospital encontrado" message with muted text
     d. Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` with HospitalCard for each paginated hospital
     e. Pagination at bottom (mt-6, centered) if totalPages > 1

2. Refactor `frontend/src/app/hospital/page.tsx`:
   - Simplify to just render HospitalGrid component
   - Remove ALL existing state management, useEffect, fetch logic, grid rendering
   - Remove the Hospital interface definition (move to a shared types file or define in HospitalGrid)
   - The page becomes a thin wrapper:
     ```typescript
     import { HospitalGrid } from '@/components/hospital/HospitalGrid'
     
     export default function HospitalPage() {
       return (
         <div className="mx-auto max-w-6xl px-6 py-8">
           <HospitalGrid />
         </div>
       )
     }
     ```
   - Note: AuthGuard wrapping is handled by hospital/layout.tsx (from Plan 02), so this page should NOT import or use AuthGuard

3. Define the Hospital interface in HospitalGrid.tsx (or create a shared types file `frontend/src/types/hospital.ts` if preferred -- Claude's discretion). The interface must match the API response:
   ```typescript
   interface Hospital {
     id: string
     name: string
     cnes: string
     logo_url: string | null
     period_start: string | null
     period_end: string | null
     powerbi_url: string | null
     sort_order: number
     created_at: string
   }
   ```
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx next build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/components/hospital/HospitalGrid.tsx
    - grep "useMemo" frontend/src/components/hospital/HospitalGrid.tsx
    - grep "HospitalCard" frontend/src/components/hospital/HospitalGrid.tsx
    - grep "SearchInput" frontend/src/components/hospital/HospitalGrid.tsx
    - grep "Pagination" frontend/src/components/hospital/HospitalGrid.tsx
    - grep "HospitalCardSkeleton" frontend/src/components/hospital/HospitalGrid.tsx
    - grep "HospitalGrid" frontend/src/app/hospital/page.tsx
    - grep "grid-cols-1" frontend/src/components/hospital/HospitalGrid.tsx
    - grep "md:grid-cols-2" frontend/src/components/hospital/HospitalGrid.tsx
    - grep "lg:grid-cols-3" frontend/src/components/hospital/HospitalGrid.tsx
  </acceptance_criteria>
  <done>HospitalGrid orchestrates card rendering with search filter, pagination, and skeleton loading. Hospital page is a thin wrapper. Responsive grid: 1/2/3 columns. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` exits 0
- HospitalCard shows logo, name, CNES, period
- Grid responsive classes: grid-cols-1 md:grid-cols-2 lg:grid-cols-3
- Search filters by name and CNES (client-side)
- Skeleton loading shown during fetch
- Pagination renders when totalPages > 1
</verification>

<success_criteria>
- All 7 GRID requirements addressed
- 5 new reusable components in components/hospital/
- Hospital page simplified to thin wrapper
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-ui/03-03-SUMMARY.md`
</output>
