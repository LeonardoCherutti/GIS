---
phase: 03-core-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppHeader.tsx
  - frontend/src/components/layout/AppFooter.tsx
  - frontend/src/components/layout/ThemeToggle.tsx
  - frontend/src/components/layout/UserMenu.tsx
  - frontend/src/app/hospital/layout.tsx
  - frontend/src/components/ui/avatar.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UX-01, UX-02]

must_haves:
  truths:
    - "Global header appears on all authenticated pages with logo, portal name, user avatar+email, dark mode toggle, and logout"
    - "Global footer appears on all authenticated pages with company branding"
    - "Dark mode toggle switches theme and preference persists across page reloads"
    - "User Google avatar and email are visible in the header"
    - "Toast notifications appear on auth events and errors"
  artifacts:
    - path: "frontend/src/components/layout/AppHeader.tsx"
      provides: "Global header with logo, user profile, theme toggle, logout"
      exports: ["default"]
    - path: "frontend/src/components/layout/AppFooter.tsx"
      provides: "Global footer with company branding"
      exports: ["default"]
    - path: "frontend/src/components/layout/ThemeToggle.tsx"
      provides: "Dark mode toggle button"
      exports: ["default"]
    - path: "frontend/src/components/layout/UserMenu.tsx"
      provides: "User avatar + dropdown with email and logout"
      exports: ["default"]
    - path: "frontend/src/app/hospital/layout.tsx"
      provides: "Shared layout wrapping hospital routes with AuthGuard, AppHeader, AppFooter"
      exports: ["default"]
  key_links:
    - from: "frontend/src/app/hospital/layout.tsx"
      to: "frontend/src/components/layout/AppHeader.tsx"
      via: "import and render"
      pattern: "import.*AppHeader"
    - from: "frontend/src/components/layout/UserMenu.tsx"
      to: "frontend/src/contexts/AuthContext.tsx"
      via: "useAuth hook"
      pattern: "useAuth"
    - from: "frontend/src/components/layout/ThemeToggle.tsx"
      to: "next-themes"
      via: "useTheme hook"
      pattern: "useTheme"
---

<objective>
Create the shared layout system: global header (logo, portal name, user profile, dark mode toggle, logout), global footer (company branding), and hospital route layout that wraps all authenticated pages.

Purpose: Currently each page renders its own header, duplicating logic. A shared layout provides consistent chrome, user profile display, dark mode toggle, and reduces code duplication. This satisfies the design system requirements (UI-01 through UI-06) and UX requirements (UX-01, UX-02).
Output: Layout components, hospital route layout.tsx, shadcn avatar and dropdown-menu installed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ui/03-RESEARCH.md

@frontend/src/app/layout.tsx (root layout with ThemeProvider, AuthProvider, Toaster)
@frontend/src/contexts/AuthContext.tsx (AuthUser: email, name, picture, sub; useAuth hook)
@frontend/src/app/styles/palette.css (CSS custom properties for light/dark)
@frontend/src/app/globals.css (theme bridge: palette vars to Tailwind colors)
@frontend/src/components/ui/button.tsx (shadcn Button)

<interfaces>
From frontend/src/contexts/AuthContext.tsx:
```typescript
interface AuthUser {
  email: string
  name: string
  picture: string
  sub: string
}
interface AuthContextType {
  user: AuthUser | null
  isAuthenticated: boolean
  isLoading: boolean
  token: string | null
  login: (credentialResponse: { credential?: string }) => Promise<void>
  logout: () => Promise<void>
}
export function useAuth(): AuthContextType
```

From D:\ferreiracontabilidade\app\src\components\layout\ThemeToggle.tsx:
```typescript
// Pattern: useTheme from next-themes, Sun/Moon from lucide-react
// Button with onClick={() => setTheme(isDark ? 'light' : 'dark')}
```

From D:\ferreiracontabilidade\app\src\components\layout\UserMenu.tsx:
```typescript
// Pattern: Avatar with AvatarImage + AvatarFallback (initials)
// Uses useAuth().user for picture, name, email
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and create layout components</name>
  <files>
    frontend/src/components/ui/avatar.tsx,
    frontend/src/components/ui/dropdown-menu.tsx,
    frontend/src/components/layout/ThemeToggle.tsx,
    frontend/src/components/layout/UserMenu.tsx,
    frontend/src/components/layout/AppHeader.tsx,
    frontend/src/components/layout/AppFooter.tsx
  </files>
  <read_first>
    - frontend/src/contexts/AuthContext.tsx (AuthUser interface, useAuth hook)
    - frontend/src/app/styles/palette.css (palette variable names)
    - frontend/src/components/ui/button.tsx (Button component API)
    - D:\ferreiracontabilidade\app\src\components\layout\ThemeToggle.tsx (reference pattern)
    - D:\ferreiracontabilidade\app\src\components\layout\UserMenu.tsx (reference pattern)
    - Read node_modules/next/dist/docs/ in frontend/ for any component/routing API changes
  </read_first>
  <action>
1. Install shadcn components:
   ```bash
   cd frontend && npx shadcn@latest add avatar dropdown-menu input -y
   ```

2. Create `frontend/src/components/layout/ThemeToggle.tsx`:
   - 'use client' component
   - Import `useTheme` from 'next-themes', `Sun` and `Moon` from 'lucide-react'
   - Render a button (use shadcn Button variant="ghost" size="icon") that toggles between 'light' and 'dark'
   - aria-label: "Mudar para tema claro" / "Mudar para tema escuro"
   - Follow ferreiracontabilidade reference pattern exactly (minus i18n -- use hardcoded Portuguese)

3. Create `frontend/src/components/layout/UserMenu.tsx`:
   - 'use client' component
   - Import Avatar, AvatarImage, AvatarFallback from shadcn
   - Import useAuth from AuthContext
   - Show Google avatar using `user.picture` in AvatarImage
   - AvatarFallback: initials from user.name (split by space, take first letter of first 2 words, uppercase)
   - Use DropdownMenu from shadcn: trigger is the avatar, content shows user.name, user.email, separator, logout button
   - Logout calls `await logout()` then `router.push('/')`
   - Import useRouter from 'next/navigation' for redirect after logout

4. Create `frontend/src/components/layout/AppHeader.tsx`:
   - 'use client' component
   - Flex row: left side = GIS logo text ("G.I.S." in bold primary color) + "Gestao Inteligente em Saude" subtitle
   - Right side = ThemeToggle + UserMenu
   - Background: var(--palette-surface-raised), border-bottom: 1px solid var(--palette-border)
   - Padding: px-6 py-3
   - Use Tailwind theme colors where possible (e.g., `bg-surface-raised border-border text-primary`)
   - Logo area: consider using the company logo image. For now, text-based logo with hospital icon from lucide-react (Hospital or Building2 icon)

5. Create `frontend/src/components/layout/AppFooter.tsx`:
   - Simple footer with centered text: "G.I.S. Gestao Inteligente em Saude" and copyright year
   - Background: var(--palette-surface), border-top: 1px solid var(--palette-border)
   - Text color: var(--palette-muted-fg), text-sm
   - Padding: py-4 px-6

Note on palette.css (per UI-01, UI-06): palette.css already exists with full light/dark themes. globals.css already bridges palette vars to Tailwind theme. The design system is ALREADY complete from Phase 1. UI-01 and UI-06 are satisfied by existing code. These new components just USE the existing palette system.

Note on toasts (per UX-02): sonner is already installed and configured in root layout.tsx with `<Toaster position="top-right" richColors closeButton duration={5000} />`. toast() is already used in hospital/page.tsx. UX-02 is already satisfied. No additional work needed.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx next build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/components/layout/ThemeToggle.tsx
    - test -f frontend/src/components/layout/UserMenu.tsx
    - test -f frontend/src/components/layout/AppHeader.tsx
    - test -f frontend/src/components/layout/AppFooter.tsx
    - grep "useTheme" frontend/src/components/layout/ThemeToggle.tsx
    - grep "useAuth" frontend/src/components/layout/UserMenu.tsx
    - grep "ThemeToggle" frontend/src/components/layout/AppHeader.tsx
    - grep "UserMenu" frontend/src/components/layout/AppHeader.tsx
    - grep "G.I.S" frontend/src/components/layout/AppFooter.tsx
    - test -f frontend/src/components/ui/avatar.tsx
  </acceptance_criteria>
  <done>All 4 layout components created. AppHeader includes ThemeToggle and UserMenu. AppFooter has company branding. shadcn avatar and dropdown-menu installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create hospital route layout and refactor pages</name>
  <files>
    frontend/src/app/hospital/layout.tsx,
    frontend/src/app/hospital/page.tsx,
    frontend/src/app/hospital/[cnes]/page.tsx
  </files>
  <read_first>
    - frontend/src/app/hospital/page.tsx (current grid page with inline header)
    - frontend/src/app/hospital/[cnes]/page.tsx (current dashboard page with inline header)
    - frontend/src/components/auth/AuthGuard.tsx (auth wrapper)
    - frontend/src/components/layout/AppHeader.tsx (just created in Task 1)
    - frontend/src/components/layout/AppFooter.tsx (just created in Task 1)
  </read_first>
  <action>
1. Create `frontend/src/app/hospital/layout.tsx`:
   - 'use client' component (needed for AuthGuard which uses useAuth)
   - Wrap children in AuthGuard
   - Render: min-h-screen flex flex-col with bg-background
   - Structure: AppHeader at top, `<main className="flex-1">{children}</main>`, AppFooter at bottom
   - This replaces per-page AuthGuard wrapping and per-page headers

2. Refactor `frontend/src/app/hospital/page.tsx`:
   - REMOVE the AuthGuard wrapper (now in layout.tsx)
   - REMOVE the inline header (logo, user name, logout button)
   - REMOVE the handleLogout function
   - Keep only the grid content inside `<main>` equivalent (the `mx-auto max-w-6xl px-6 py-8` container)
   - REMOVE the outer min-h-screen div with background (layout handles this)
   - Keep: useState for hospitals/loading, useEffect for fetchHospitals, the grid rendering, the loading spinner, the empty state
   - The component should just export a function that renders the grid content (no AuthGuard wrapper, no page-level header)

3. Refactor `frontend/src/app/hospital/[cnes]/page.tsx`:
   - REMOVE the AuthGuard wrapper (now in layout.tsx)
   - REMOVE the inline header with hospital name and back button (will be handled in Plan 04 with dashboard-specific header content)
   - For now, keep a simple back link and hospital name at top of content area
   - REMOVE outer min-h-screen div with background (layout handles this)
   - Keep: the use() hook for params, useState, useEffect for fetchHospital, loading/notFound states
   - The Power BI placeholder content stays for now (Plan 04 replaces it with actual iframe)
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx next build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/app/hospital/layout.tsx
    - grep "AuthGuard" frontend/src/app/hospital/layout.tsx
    - grep "AppHeader" frontend/src/app/hospital/layout.tsx
    - grep "AppFooter" frontend/src/app/hospital/layout.tsx
    - grep -L "AuthGuard" frontend/src/app/hospital/page.tsx (should NOT contain AuthGuard)
    - grep -L "handleLogout" frontend/src/app/hospital/page.tsx (should NOT contain handleLogout)
  </acceptance_criteria>
  <done>Hospital layout.tsx wraps all hospital routes with AuthGuard, AppHeader, AppFooter. Grid page and dashboard page no longer have inline headers or AuthGuard wrappers. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` exits 0
- AppHeader renders ThemeToggle and UserMenu
- hospital/layout.tsx wraps with AuthGuard + AppHeader + AppFooter
- hospital/page.tsx has no inline header or AuthGuard
- hospital/[cnes]/page.tsx has no inline header or AuthGuard
</verification>

<success_criteria>
- Shared layout components exist and build successfully
- All authenticated pages share the same header/footer chrome
- Dark mode toggle uses next-themes and persists preference
- User avatar and email visible in header via UserMenu dropdown
- Toast system already functional (sonner configured in root layout)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-ui/03-02-SUMMARY.md`
</output>
