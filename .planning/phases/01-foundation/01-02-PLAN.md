---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/next.config.ts
  - frontend/tsconfig.json
  - frontend/postcss.config.mjs
  - frontend/components.json
  - frontend/src/app/globals.css
  - frontend/src/app/styles/palette.css
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/api/proxy/[...path]/route.ts
  - frontend/src/app/api/session/route.ts
  - frontend/src/lib/utils.ts
  - frontend/src/lib/theme-colors.ts
  - frontend/src/lib/api/client.ts
  - frontend/src/providers/QueryProvider.tsx
  - frontend/src/contexts/AuthContext.tsx
autonomous: true
requirements: [INFRA-04, SPA-01, SPA-02]

must_haves:
  truths:
    - "Next.js app builds with output standalone without errors"
    - "SPA loads in browser with provider stack (ThemeProvider > AuthProvider > QueryProvider)"
    - "Proxy route forwards requests from /api/proxy/* to Go API URL"
    - "palette.css defines CSS custom properties for light and dark themes"
    - "globals.css imports tailwind, palette, shadcn in correct order with @custom-variant dark"
  artifacts:
    - path: "frontend/next.config.ts"
      provides: "Next.js config with standalone output and COOP header"
      contains: "output: 'standalone'"
    - path: "frontend/src/app/globals.css"
      provides: "Tailwind + palette + shadcn CSS integration"
      contains: "@custom-variant dark"
    - path: "frontend/src/app/styles/palette.css"
      provides: "CSS custom properties for light/dark healthcare theme"
      contains: "--palette-primary"
    - path: "frontend/src/app/layout.tsx"
      provides: "Root layout with ThemeProvider > AuthProvider > QueryProvider stack"
      contains: "ThemeProvider"
    - path: "frontend/src/app/api/proxy/[...path]/route.ts"
      provides: "Catch-all proxy to Go API"
      contains: "auth-session"
    - path: "frontend/src/lib/api/client.ts"
      provides: "Typed apiFetch<T>() with Result type"
      contains: "Result<T"
    - path: "frontend/src/contexts/AuthContext.tsx"
      provides: "Auth context shell for Phase 2"
      contains: "AuthProvider"
  key_links:
    - from: "frontend/src/app/globals.css"
      to: "frontend/src/app/styles/palette.css"
      via: "@import ./styles/palette.css"
      pattern: "@import.*palette"
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/contexts/AuthContext.tsx"
      via: "import AuthProvider"
      pattern: "AuthProvider"
    - from: "frontend/src/app/api/proxy/[...path]/route.ts"
      to: "Go API"
      via: "fetch to API_URL"
      pattern: "API_URL"
    - from: "frontend/src/lib/api/client.ts"
      to: "frontend/src/app/api/proxy/[...path]/route.ts"
      via: "fetch to /api/proxy"
      pattern: "/api/proxy"
---

<objective>
Scaffold the Next.js frontend with SPA patterns matching the ferreiracontabilidade reference project: app router, palette.css theming, provider stack, proxy route, session route shell, and API client.

Purpose: Establishes the frontend foundation that all UI phases build upon, with exact pattern fidelity to the reference project.
Output: Buildable Next.js app with standalone output, theming, providers, proxy route, and API client library.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Reference project patterns — executor MUST replicate these exactly -->

From D:\ferreiracontabilidade\app\next.config.ts (adapt for GIS — no next-intl plugin in Phase 1):
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  output: 'standalone',
  async headers() {
    return [{
      source: '/(.*)',
      headers: [
        { key: 'Cross-Origin-Opener-Policy', value: 'same-origin-allow-popups' }
      ]
    }]
  },
}

export default nextConfig
```

From D:\ferreiracontabilidade\app\src\app\layout.tsx (provider order):
```tsx
<ThemeProvider attribute="data-theme" defaultTheme="light" enableSystem={false}>
  <AuthProvider>
    <QueryProvider>
      {children}
    </QueryProvider>
    <Toaster position="top-right" richColors closeButton duration={5000} />
  </AuthProvider>
</ThemeProvider>
```

From D:\ferreiracontabilidade\app\src\app\globals.css (import order and @custom-variant):
```css
@import "tailwindcss";
@import "./styles/palette.css";
@import "tw-animate-css";
@import "shadcn/tailwind.css";

@custom-variant dark (&:is([data-theme="dark"] *));
```

From D:\ferreiracontabilidade\app\src\lib\utils.ts:
```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

From D:\ferreiracontabilidade\app\src\lib\api\client.ts:
```typescript
export type Result<T, E = ApiError> =
  | { ok: true; data: T }
  | { ok: false; error: E }

export interface ApiError {
  status: number
  message: string
  code?: string
}

export async function apiFetch<T>(
  path: string,
  options?: RequestInit & { token?: string }
): Promise<Result<T>> {
  // ... fetches via /api/proxy
}
```

From D:\ferreiracontabilidade\app\src\providers\QueryProvider.tsx:
```tsx
'use client'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { useState, ReactNode } from 'react'

export default function QueryProvider({ children }: { children: ReactNode }) {
  const [queryClient] = useState(() => new QueryClient({
    defaultOptions: { queries: { staleTime: 30_000, retry: 1 } },
  }))
  return <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
}
```

From D:\ferreiracontabilidade\app\src\app\api\proxy\[...path]\route.ts (adapt cookie name):
- Cookie name: `auth-session` (NOT `firebase-session`)
- No serpro_token forwarding needed
- API_URL defaults to `http://api:8080`

From D:\ferreiracontabilidade\app\src\app\api\session\route.ts (adapt cookie name):
- Cookie name: `auth-session` (NOT `firebase-session`)
- POST sets httpOnly cookie, DELETE clears it

From D:\ferreiracontabilidade\app\components.json:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "base-nova",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "iconLibrary": "lucide",
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  }
}
```

Palette.css for GIS (healthcare theme):
```css
:root {
  --palette-primary:        #094344;
  --palette-primary-hover:  #0c5657;
  --palette-primary-subtle: #e8f2f2;
  --palette-accent:         #f05483;
  --palette-accent-hover:   #e03b6a;
  --palette-background:     #ffffff;
  --palette-surface:        #f8fafb;
  --palette-surface-raised: #ffffff;
  --palette-border:         #e2e8ea;
  --palette-destructive:    #dc2626;
  --palette-destructive-fg: #ffffff;
  --palette-foreground:     #0a1628;
  --palette-muted-fg:       #6b7280;
  --palette-disabled-fg:    #9ca3af;
}

[data-theme="dark"] {
  --palette-background:     #163536;
  --palette-surface:        #1c4142;
  --palette-surface-raised: #224d4e;
  --palette-border:         #2d5e60;
  --palette-primary:        #0eb8bc;
  --palette-primary-hover:  #12d4d8;
  --palette-primary-subtle: #1a3a3b;
  --palette-accent:         #f05483;
  --palette-accent-hover:   #e03b6a;
  --palette-foreground:     #f0f4f4;
  --palette-muted-fg:       #8fa8aa;
  --palette-disabled-fg:    #4a6869;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with dependencies and configuration files</name>
  <files>
    frontend/package.json
    frontend/next.config.ts
    frontend/tsconfig.json
    frontend/postcss.config.mjs
    frontend/components.json
  </files>
  <read_first>
    D:\ferreiracontabilidade\app\package.json (source of truth for dependency versions)
    D:\ferreiracontabilidade\app\next.config.ts (pattern for standalone + COOP header)
    D:\ferreiracontabilidade\app\tsconfig.json (pattern for paths and compiler options)
    D:\ferreiracontabilidade\app\postcss.config.mjs (pattern for Tailwind v4 postcss)
    D:\ferreiracontabilidade\app\components.json (pattern for shadcn config)
    .planning/phases/01-foundation/01-RESEARCH.md (lines 75-83 for installation commands)
  </read_first>
  <action>
    1. Create Next.js project:
       ```bash
       npx create-next-app@latest frontend --typescript --tailwind --eslint --app --src-dir --no-turbopack
       ```
       Note: If interactive prompts appear, the defaults should match. If create-next-app is not available or fails, manually create the project structure.

    2. Install exact dependency versions matching the reference project:
       ```bash
       cd frontend
       npm install next-themes@^0.4.6 sonner@^2.0.7 lucide-react@^1.7.0
       npm install clsx@^2.1.1 tailwind-merge@^3.5.0 class-variance-authority@^0.7.1 tw-animate-css@^1.4.0
       npm install @tanstack/react-query@^5.96.0
       ```
       Do NOT install @react-oauth/google, jose, next-intl, or zustand yet (Phase 2+).

    3. Overwrite `frontend/next.config.ts` with:
       ```typescript
       import type { NextConfig } from 'next'

       const nextConfig: NextConfig = {
         output: 'standalone',
         async headers() {
           return [{
             source: '/(.*)',
             headers: [
               { key: 'Cross-Origin-Opener-Policy', value: 'same-origin-allow-popups' }
             ]
           }]
         },
       }

       export default nextConfig
       ```
       Note: NO next-intl plugin wrapping in Phase 1. Keep it simple.

    4. Ensure `frontend/tsconfig.json` has `@/*` path alias pointing to `./src/*` (create-next-app should set this, verify).

    5. Overwrite `frontend/postcss.config.mjs` with:
       ```javascript
       const config = {
         plugins: {
           "@tailwindcss/postcss": {},
         },
       };
       export default config;
       ```

    6. Create `frontend/components.json` with:
       ```json
       {
         "$schema": "https://ui.shadcn.com/schema.json",
         "style": "base-nova",
         "rsc": true,
         "tsx": true,
         "tailwind": {
           "config": "",
           "css": "src/app/globals.css",
           "baseColor": "neutral",
           "cssVariables": true,
           "prefix": ""
         },
         "iconLibrary": "lucide",
         "rtl": false,
         "aliases": {
           "components": "@/components",
           "utils": "@/lib/utils",
           "ui": "@/components/ui",
           "lib": "@/lib",
           "hooks": "@/hooks"
         },
         "registries": {}
       }
       ```

    7. Run `npx shadcn@latest init --yes` to set up shadcn (this uses components.json). If it prompts, accept defaults.

    8. Add a few base shadcn components for later use:
       ```bash
       npx shadcn@latest add button card skeleton
       ```
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"next"` in dependencies
    - `frontend/package.json` contains `"next-themes"` in dependencies
    - `frontend/package.json` contains `"sonner"` in dependencies
    - `frontend/package.json` contains `"@tanstack/react-query"` in dependencies
    - `frontend/package.json` contains `"clsx"` in dependencies
    - `frontend/package.json` contains `"tailwind-merge"` in dependencies
    - `frontend/package.json` contains `"tw-animate-css"` in dependencies
    - `frontend/next.config.ts` contains `output: 'standalone'`
    - `frontend/next.config.ts` contains `Cross-Origin-Opener-Policy`
    - `frontend/next.config.ts` contains `same-origin-allow-popups`
    - `frontend/next.config.ts` does NOT contain `next-intl` (Phase 4)
    - `frontend/postcss.config.mjs` contains `@tailwindcss/postcss`
    - `frontend/components.json` contains `"style": "base-nova"`
    - `frontend/components.json` contains `"css": "src/app/globals.css"`
    - `frontend/tsconfig.json` contains `"@/*": ["./src/*"]`
    - `frontend/src/components/ui/button.tsx` exists (shadcn component)
    - `frontend/src/components/ui/card.tsx` exists (shadcn component)
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>Next.js project initialized with all Phase 1 dependencies, standalone output config, COOP header, shadcn with base-nova style, and base UI components.</done>
</task>

<task type="auto">
  <name>Task 2: Create palette.css, globals.css, layout with providers, proxy route, session route, and utility files</name>
  <files>
    frontend/src/app/styles/palette.css
    frontend/src/app/globals.css
    frontend/src/app/layout.tsx
    frontend/src/app/page.tsx
    frontend/src/app/api/proxy/[...path]/route.ts
    frontend/src/app/api/session/route.ts
    frontend/src/lib/utils.ts
    frontend/src/lib/theme-colors.ts
    frontend/src/lib/api/client.ts
    frontend/src/providers/QueryProvider.tsx
    frontend/src/contexts/AuthContext.tsx
  </files>
  <read_first>
    D:\ferreiracontabilidade\app\src\app\globals.css (EXACT source of truth for CSS structure)
    D:\ferreiracontabilidade\app\src\app\styles\palette.css (pattern for palette variables)
    D:\ferreiracontabilidade\app\src\app\layout.tsx (EXACT source of truth for provider stack order)
    D:\ferreiracontabilidade\app\src\app\api\proxy\[...path]\route.ts (EXACT proxy pattern — adapt cookie name)
    D:\ferreiracontabilidade\app\src\app\api\session\route.ts (EXACT session pattern — adapt cookie name)
    D:\ferreiracontabilidade\app\src\lib\utils.ts (cn utility)
    D:\ferreiracontabilidade\app\src\lib\theme-colors.ts (palette keys and generatePalette)
    D:\ferreiracontabilidade\app\src\lib\api\client.ts (Result type and apiFetch)
    D:\ferreiracontabilidade\app\src\providers\QueryProvider.tsx (query client setup)
    D:\ferreiracontabilidade\app\src\contexts\AuthContext.tsx (auth context pattern — create shell only)
    frontend/src/app/globals.css (current state after Task 1 / shadcn init)
    frontend/src/app/layout.tsx (current state after Task 1)
    frontend/src/lib/utils.ts (current state after Task 1 / shadcn init)
  </read_first>
  <action>
    Create all core frontend files matching the reference project patterns. Read each reference file FIRST, then create the GIS version.

    1. Create `frontend/src/app/styles/palette.css` with GIS healthcare colors:
       ```css
       :root {
         --palette-primary:        #094344;
         --palette-primary-hover:  #0c5657;
         --palette-primary-subtle: #e8f2f2;
         --palette-accent:         #f05483;
         --palette-accent-hover:   #e03b6a;
         --palette-background:     #ffffff;
         --palette-surface:        #f8fafb;
         --palette-surface-raised: #ffffff;
         --palette-border:         #e2e8ea;
         --palette-destructive:    #dc2626;
         --palette-destructive-fg: #ffffff;
         --palette-foreground:     #0a1628;
         --palette-muted-fg:       #6b7280;
         --palette-disabled-fg:    #9ca3af;
       }

       [data-theme="dark"] {
         --palette-background:     #163536;
         --palette-surface:        #1c4142;
         --palette-surface-raised: #224d4e;
         --palette-border:         #2d5e60;
         --palette-primary:        #0eb8bc;
         --palette-primary-hover:  #12d4d8;
         --palette-primary-subtle: #1a3a3b;
         --palette-accent:         #f05483;
         --palette-accent-hover:   #e03b6a;
         --palette-foreground:     #f0f4f4;
         --palette-muted-fg:       #8fa8aa;
         --palette-disabled-fg:    #4a6869;
       }
       ```

    2. Overwrite `frontend/src/app/globals.css` to match the reference project EXACTLY (read the reference first). The structure must be:
       - `@import "tailwindcss";`
       - `@import "./styles/palette.css";`
       - `@import "tw-animate-css";`
       - `@import "shadcn/tailwind.css";`
       - `@custom-variant dark (&:is([data-theme="dark"] *));`
       - `@theme { ... }` block mapping `--color-*` to `--palette-*` variables
       - `@theme inline { ... }` block with shadcn CSS variable mappings
       - `:root { ... }` block mapping `--background`, `--foreground`, `--card`, etc. to `--palette-*`
       - `[data-theme="dark"] { ... }` block with dark mode shadcn mappings
       - `@layer base { ... }` with body/html/select base styles

       IMPORTANT: Copy the FULL globals.css structure from `D:\ferreiracontabilidade\app\src\app\globals.css`. Do NOT use a simplified version. The @theme inline block with sidebar, chart, radius variables is REQUIRED for shadcn components to render correctly.

       Remove the `--font-heading` and `--font-mono` lines from `@theme` if those font variables are not yet defined (or map them to fallback values). Keep `--font-sans: var(--font-sans);` only.

    3. Create `frontend/src/contexts/AuthContext.tsx` as a SHELL (Phase 2 fills in OAuth logic):
       ```tsx
       'use client'

       import { createContext, useContext, ReactNode } from 'react'

       interface AuthContextType {
         user: null
         isAuthenticated: boolean
         isLoading: boolean
         login: () => void
         logout: () => void
       }

       const AuthContext = createContext<AuthContextType>({
         user: null,
         isAuthenticated: false,
         isLoading: false,
         login: () => {},
         logout: () => {},
       })

       export function AuthProvider({ children }: { children: ReactNode }) {
         return (
           <AuthContext.Provider value={{
             user: null,
             isAuthenticated: false,
             isLoading: false,
             login: () => {},
             logout: () => {},
           }}>
             {children}
           </AuthContext.Provider>
         )
       }

       export function useAuth() {
         return useContext(AuthContext)
       }
       ```

    4. Create `frontend/src/providers/QueryProvider.tsx` matching reference EXACTLY:
       ```tsx
       'use client'

       import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
       import { useState, ReactNode } from 'react'

       export default function QueryProvider({ children }: { children: ReactNode }) {
         const [queryClient] = useState(() => new QueryClient({
           defaultOptions: {
             queries: {
               staleTime: 30_000,
               retry: 1,
             },
           },
         }))

         return (
           <QueryClientProvider client={queryClient}>
             {children}
           </QueryClientProvider>
         )
       }
       ```

    5. Overwrite `frontend/src/app/layout.tsx` with provider stack matching reference order (ThemeProvider > AuthProvider > QueryProvider + Toaster):
       ```tsx
       import type { Metadata } from "next"
       import { Roboto } from "next/font/google"
       import { ThemeProvider } from "next-themes"
       import { Toaster } from "sonner"
       import { AuthProvider } from "@/contexts/AuthContext"
       import QueryProvider from "@/providers/QueryProvider"
       import "./globals.css"

       const roboto = Roboto({
         variable: "--font-roboto",
         subsets: ["latin"],
         weight: ["300", "400", "500", "700"],
       })

       export const metadata: Metadata = {
         title: "GIS - Gestao Inteligente em Saude",
         description: "Portal de dashboards hospitalares",
       }

       export default function RootLayout({
         children,
       }: Readonly<{
         children: React.ReactNode
       }>) {
         return (
           <html suppressHydrationWarning className={roboto.variable}>
             <body className="antialiased">
               <ThemeProvider attribute="data-theme" defaultTheme="light" enableSystem={false}>
                 <AuthProvider>
                   <QueryProvider>
                     {children}
                   </QueryProvider>
                   <Toaster position="top-right" richColors closeButton duration={5000} />
                 </AuthProvider>
               </ThemeProvider>
             </body>
           </html>
         )
       }
       ```

    6. Overwrite `frontend/src/app/page.tsx` with a minimal placeholder:
       ```tsx
       export default function Home() {
         return (
           <main className="flex min-h-screen items-center justify-center bg-background text-foreground">
             <div className="text-center">
               <h1 className="text-4xl font-bold text-primary">GIS</h1>
               <p className="mt-2 text-muted-fg">Gestao Inteligente em Saude</p>
             </div>
           </main>
         )
       }
       ```

    7. Create `frontend/src/app/api/proxy/[...path]/route.ts` adapted from reference:
       - Cookie name: `auth-session` (NOT `firebase-session`)
       - No serpro_token forwarding
       - `API_URL` defaults to `http://api:8080`
       - Forward GET, POST, PUT, PATCH, DELETE methods
       - Extract session cookie and forward as `Authorization: Bearer` header
       - Set `maxDuration = 120`

    8. Create `frontend/src/app/api/session/route.ts` adapted from reference:
       - Cookie name: `auth-session` (NOT `firebase-session`)
       - POST sets httpOnly, secure (in production), sameSite lax, 7-day maxAge cookie
       - DELETE clears cookie with maxAge 0

    9. Ensure `frontend/src/lib/utils.ts` contains the cn() utility (shadcn init may have created it; verify it matches):
       ```typescript
       import { clsx, type ClassValue } from "clsx"
       import { twMerge } from "tailwind-merge"

       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs))
       }
       ```

    10. Create `frontend/src/lib/theme-colors.ts` copied from reference project:
        - `DEFAULT_THEME_COLOR` constant (null)
        - `PALETTE_KEYS` array of all `--palette-*` CSS variable names
        - `generatePalette(hex)` function returning light/dark palette objects
        - Copy the hexToHsl, hslToHex, clamp, adjustLightness helper functions

    11. Create `frontend/src/lib/api/client.ts` adapted from reference:
        - `Result<T, E>` discriminated union type
        - `ApiError` interface with status, message, code
        - `apiFetch<T>()` function that fetches via `/api/proxy` prefix
        - Error messages in Portuguese: "Erro desconhecido", "Sem conexao. Verifique sua internet e tente novamente."

    12. After creating all files, run `cd frontend && npx next build` to verify everything compiles.
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/styles/palette.css` contains `--palette-primary:        #094344`
    - `frontend/src/app/styles/palette.css` contains `[data-theme="dark"]`
    - `frontend/src/app/styles/palette.css` contains `--palette-primary:        #0eb8bc` (dark mode primary)
    - `frontend/src/app/globals.css` first line is `@import "tailwindcss";`
    - `frontend/src/app/globals.css` contains `@import "./styles/palette.css";`
    - `frontend/src/app/globals.css` contains `@import "tw-animate-css";`
    - `frontend/src/app/globals.css` contains `@import "shadcn/tailwind.css";`
    - `frontend/src/app/globals.css` contains `@custom-variant dark (&:is([data-theme="dark"] *));`
    - `frontend/src/app/globals.css` contains `--color-primary:          var(--palette-primary);`
    - `frontend/src/app/globals.css` contains `--background: var(--palette-background);`
    - `frontend/src/app/globals.css` contains `--card: var(--palette-surface-raised);`
    - `frontend/src/app/layout.tsx` contains `ThemeProvider attribute="data-theme"`
    - `frontend/src/app/layout.tsx` contains `<AuthProvider>`
    - `frontend/src/app/layout.tsx` contains `<QueryProvider>`
    - `frontend/src/app/layout.tsx` contains `<Toaster position="top-right"`
    - `frontend/src/app/layout.tsx` contains `suppressHydrationWarning`
    - `frontend/src/app/api/proxy/[...path]/route.ts` contains `auth-session`
    - `frontend/src/app/api/proxy/[...path]/route.ts` contains `API_URL`
    - `frontend/src/app/api/proxy/[...path]/route.ts` contains `Authorization`
    - `frontend/src/app/api/proxy/[...path]/route.ts` contains `export const GET = proxy`
    - `frontend/src/app/api/session/route.ts` contains `auth-session`
    - `frontend/src/app/api/session/route.ts` contains `httpOnly: true`
    - `frontend/src/lib/utils.ts` contains `export function cn`
    - `frontend/src/lib/api/client.ts` contains `export type Result<T`
    - `frontend/src/lib/api/client.ts` contains `export async function apiFetch`
    - `frontend/src/lib/api/client.ts` contains `/api/proxy`
    - `frontend/src/lib/theme-colors.ts` contains `PALETTE_KEYS`
    - `frontend/src/lib/theme-colors.ts` contains `generatePalette`
    - `frontend/src/providers/QueryProvider.tsx` contains `'use client'`
    - `frontend/src/providers/QueryProvider.tsx` contains `QueryClientProvider`
    - `frontend/src/contexts/AuthContext.tsx` contains `export function AuthProvider`
    - `frontend/src/contexts/AuthContext.tsx` contains `export function useAuth`
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Complete Next.js frontend scaffold with palette.css theming, globals.css with Tailwind v4 + shadcn integration, root layout with ThemeProvider > AuthProvider > QueryProvider stack, catch-all proxy route to Go API, session route shell, cn() utility, apiFetch client with Result type, and theme-colors utility. All matching ferreiracontabilidade reference patterns.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` succeeds with standalone output
- `grep -r "auth-session" frontend/src/` shows matches in proxy route and session route
- `grep "data-theme" frontend/src/app/layout.tsx` confirms theme attribute
- `grep "@custom-variant dark" frontend/src/app/globals.css` confirms dark mode setup
- `grep "palette-primary" frontend/src/app/styles/palette.css | head -2` shows both light (#094344) and dark (#0eb8bc) values
</verification>

<success_criteria>
- Next.js builds successfully with `output: 'standalone'`
- COOP header configured for Google OAuth popup support
- palette.css has all CSS custom properties for light and dark healthcare themes
- globals.css imports are in correct order: tailwindcss > palette > tw-animate > shadcn
- @custom-variant dark maps to data-theme="dark" attribute (not class-based)
- Root layout provider stack: ThemeProvider > AuthProvider > QueryProvider + Toaster
- Proxy route forwards to Go API using auth-session cookie as Bearer token
- Session route manages auth-session httpOnly cookie (set/delete)
- API client provides typed apiFetch<T>() with Result discriminated union
- AuthContext shell exists ready for Phase 2 OAuth implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
