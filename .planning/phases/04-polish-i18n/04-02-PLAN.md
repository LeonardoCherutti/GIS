---
phase: 04-polish-i18n
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/app/[locale]/page.tsx
  - frontend/src/app/[locale]/hospital/[cnes]/page.tsx
  - frontend/src/components/layout/AppHeader.tsx
  - frontend/src/components/layout/AppFooter.tsx
  - frontend/src/components/layout/UserMenu.tsx
  - frontend/src/components/layout/ThemeToggle.tsx
  - frontend/src/components/auth/LoginButton.tsx
  - frontend/src/components/auth/AuthGuard.tsx
  - frontend/src/components/hospital/HospitalGrid.tsx
  - frontend/src/components/hospital/HospitalCard.tsx
  - frontend/src/components/hospital/SearchInput.tsx
  - frontend/src/components/hospital/Pagination.tsx
  - frontend/src/components/dashboard/DashboardEmbed.tsx
  - frontend/src/components/dashboard/DashboardErrorBoundary.tsx
  - frontend/src/components/dashboard/FullscreenToggle.tsx
autonomous: true
requirements:
  - I18N-02

must_haves:
  truths:
    - "Zero hardcoded Portuguese strings exist in component JSX -- all visible text comes from useTranslations"
    - "Every component with user-visible text imports and calls useTranslations with appropriate namespace"
    - "Message keys in JSON files match exactly what components reference via t() calls"
  artifacts:
    - path: "frontend/src/components/layout/AppHeader.tsx"
      provides: "Translated header"
      contains: "useTranslations"
    - path: "frontend/src/components/layout/AppFooter.tsx"
      provides: "Translated footer"
      contains: "useTranslations"
    - path: "frontend/src/components/auth/LoginButton.tsx"
      provides: "Translated login button and errors"
      contains: "useTranslations"
    - path: "frontend/src/components/hospital/HospitalGrid.tsx"
      provides: "Translated no-results message"
      contains: "useTranslations"
    - path: "frontend/src/components/hospital/HospitalCard.tsx"
      provides: "Translated CNES label, locale-aware month formatting"
      contains: "useTranslations"
    - path: "frontend/src/components/dashboard/DashboardErrorBoundary.tsx"
      provides: "Translated error messages"
      contains: "useTranslations"
  key_links:
    - from: "frontend/src/components/layout/AppHeader.tsx"
      to: "frontend/src/messages/pt-BR/common.json"
      via: "useTranslations('common')"
      pattern: "useTranslations.*common"
    - from: "frontend/src/components/auth/LoginButton.tsx"
      to: "frontend/src/messages/pt-BR/auth.json"
      via: "useTranslations('auth')"
      pattern: "useTranslations.*auth"
    - from: "frontend/src/components/hospital/HospitalGrid.tsx"
      to: "frontend/src/messages/pt-BR/hospital.json"
      via: "useTranslations('hospital')"
      pattern: "useTranslations.*hospital"
    - from: "frontend/src/components/dashboard/DashboardEmbed.tsx"
      to: "frontend/src/messages/pt-BR/dashboard.json"
      via: "useTranslations('dashboard')"
      pattern: "useTranslations.*dashboard"
---

<objective>
Replace every hardcoded Portuguese string in all components with useTranslations() calls referencing the pt-BR message files created in Plan 01.

Purpose: Eliminates all hardcoded strings so the UI is fully driven by message files (I18N-02).
Output: All 15 components updated to use useTranslations(), zero Portuguese literals in JSX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polish-i18n/04-CONTEXT.md
@.planning/phases/04-polish-i18n/04-RESEARCH.md
@.planning/phases/04-polish-i18n/04-01-SUMMARY.md

<interfaces>
<!-- Message file keys created in Plan 01. Use these exact keys in useTranslations() calls. -->

common.json namespace "common":
- common.brand.short = "G.I.S."
- common.brand.full = "Gestao Inteligente em Saude"
- common.brand.tagline = "Portal de Dashboards Hospitalares"
- common.nav.avatar = "Avatar"
- common.nav.logout = "Sair"
- common.theme.switchToLight = "Mudar para tema claro"
- common.theme.switchToDark = "Mudar para tema escuro"
- common.footer.copyright = "G.I.S. Gestao Inteligente em Saude (c) {year}"
- common.loading = "Carregando..."

auth.json namespace "auth":
- auth.loginButton = "Entrar com Google"
- auth.redirecting = "Redirecionando..."
- auth.errors.googleNotLoaded = "Servico do Google nao carregado. Recarregue a pagina."
- auth.errors.generic = "Erro ao autenticar"
- auth.errors.popupBlocked = "Popup bloqueado. Habilite popups para este site."

hospital.json namespace "hospital":
- hospital.searchPlaceholder = "Buscar por nome ou CNES..."
- hospital.noResults = "Nenhum hospital encontrado."
- hospital.cnesLabel = "CNES {code}"
- hospital.notFound = "Hospital nao encontrado"
- hospital.backToList = "Voltar para a lista"
- hospital.backToHospitals = "Voltar para hospitais"
- hospital.pagination.label = "Paginacao"
- hospital.pagination.previous = "Pagina anterior"
- hospital.pagination.next = "Proxima pagina"

dashboard.json namespace "dashboard":
- dashboard.loading = "Carregando dashboard..."
- dashboard.notConfigured = "Dashboard nao configurado para este hospital."
- dashboard.error.title = "Erro ao carregar dashboard"
- dashboard.error.description = "O dashboard nao pode ser carregado. Verifique sua conexao e tente novamente."
- dashboard.error.retry = "Tentar novamente"
- dashboard.fullscreen.enter = "Tela cheia"
- dashboard.fullscreen.exit = "Sair da tela cheia"

Usage pattern (all components are 'use client'):
```typescript
import { useTranslations } from 'next-intl'
// In component body:
const t = useTranslations('namespace')
// Then: t('key') or t('nested.key') or t('key', { variable: value })
```
</interfaces>

**CRITICAL Next.js 16 Notes:**
- Read `node_modules/next/dist/docs/` before writing code -- APIs may differ from training data
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract strings from layout, auth, and common components</name>
  <files>
    frontend/src/components/layout/AppHeader.tsx
    frontend/src/components/layout/AppFooter.tsx
    frontend/src/components/layout/UserMenu.tsx
    frontend/src/components/layout/ThemeToggle.tsx
    frontend/src/components/auth/LoginButton.tsx
    frontend/src/components/auth/AuthGuard.tsx
    frontend/src/app/[locale]/page.tsx
  </files>
  <read_first>
    frontend/src/components/layout/AppHeader.tsx
    frontend/src/components/layout/AppFooter.tsx
    frontend/src/components/layout/UserMenu.tsx
    frontend/src/components/layout/ThemeToggle.tsx
    frontend/src/components/auth/LoginButton.tsx
    frontend/src/components/auth/AuthGuard.tsx
    frontend/src/app/[locale]/page.tsx
    frontend/src/messages/pt-BR/common.json
    frontend/src/messages/pt-BR/auth.json
  </read_first>
  <action>
Add `import { useTranslations } from 'next-intl'` to each component and replace hardcoded strings with t() calls. Specific changes per file:

**AppHeader.tsx** -- namespace: `common`
- Add `const t = useTranslations('common')` inside component
- Replace `"G.I.S."` with `{t('brand.short')}`
- Replace `"Gestao Inteligente em Saude"` with `{t('brand.full')}`

**AppFooter.tsx** -- namespace: `common`
- Must become a client component: add `'use client'` at top
- Add `import { useTranslations } from 'next-intl'`
- Add `const t = useTranslations('common')`
- Replace the copyright paragraph with `{t('footer.copyright', { year: new Date().getFullYear() })}`

**UserMenu.tsx** -- namespace: `common`
- Add `const t = useTranslations('common')`
- Replace `alt={user.name ?? 'Avatar'}` with `alt={user.name ?? t('nav.avatar')}`
- Replace `"Sair"` text with `{t('nav.logout')}`

**ThemeToggle.tsx** -- namespace: `common`
- Add `const t = useTranslations('common')`
- Replace `aria-label={isDark ? 'Mudar para tema claro' : 'Mudar para tema escuro'}` with `aria-label={isDark ? t('theme.switchToLight') : t('theme.switchToDark')}`

**LoginButton.tsx** -- namespace: `auth`
- Add `const t = useTranslations('auth')`
- Replace `'Servico do Google nao carregado. Recarregue a pagina.'` with `t('errors.googleNotLoaded')`
- Replace `'Erro ao autenticar'` (fallback in catch) with `t('errors.generic')`
- Replace `'Popup bloqueado. Habilite popups para este site.'` with `t('errors.popupBlocked')`
- Replace `"Entrar com Google"` with `{t('loginButton')}`

**AuthGuard.tsx** -- namespace: `common`
- Add `import { useTranslations } from 'next-intl'`
- Add `const t = useTranslations('common')`
- Replace `"Carregando..."` with `{t('loading')}`

**app/[locale]/page.tsx** (login page) -- namespaces: `common` and `auth`
- Add `const tc = useTranslations('common')` and `const ta = useTranslations('auth')`
- Replace `"Carregando..."` with `{tc('loading')}`
- Replace `"Redirecionando..."` with `{ta('redirecting')}`
- Replace `"GIS"` heading with `{tc('brand.short')}`
- Replace `"Gestao Inteligente em Saude"` with `{tc('brand.full')}`
- Replace `"Portal de Dashboards Hospitalares"` with `{tc('brand.tagline')}`

**Do NOT modify** the static `metadata` export in app/layout.tsx -- keep it hardcoded per research recommendation (metadata limitation).
  </action>
  <verify>
    <automated>cd frontend && grep -rn "Gestao Inteligente\|Entrar com Google\|Carregando\.\.\.\|Sair\b\|Mudar para tema\|Popup bloqueado\|Erro ao autenticar\|Redirecionando\|Portal de Dashboards" src/components/layout/ src/components/auth/ src/app/\[locale\]/page.tsx 2>/dev/null | grep -v "node_modules" | grep -v ".json" | grep -v "useTranslations" | grep -v "import" || echo "PASS: No hardcoded Portuguese strings found in layout/auth components"</automated>
  </verify>
  <done>
    - All 7 files import useTranslations from next-intl
    - AppHeader uses t('brand.short') and t('brand.full')
    - AppFooter uses t('footer.copyright', { year }) with interpolation
    - UserMenu uses t('nav.logout') and t('nav.avatar')
    - ThemeToggle uses t('theme.switchToLight') and t('theme.switchToDark')
    - LoginButton uses t('loginButton'), t('errors.googleNotLoaded'), t('errors.generic'), t('errors.popupBlocked')
    - AuthGuard uses t('loading')
    - Login page uses tc('brand.*'), tc('loading'), ta('redirecting')
    - Zero hardcoded Portuguese strings remain in these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract strings from hospital and dashboard components</name>
  <files>
    frontend/src/components/hospital/HospitalGrid.tsx
    frontend/src/components/hospital/HospitalCard.tsx
    frontend/src/components/hospital/SearchInput.tsx
    frontend/src/components/hospital/Pagination.tsx
    frontend/src/components/dashboard/DashboardEmbed.tsx
    frontend/src/components/dashboard/DashboardErrorBoundary.tsx
    frontend/src/components/dashboard/FullscreenToggle.tsx
    frontend/src/app/[locale]/hospital/[cnes]/page.tsx
  </files>
  <read_first>
    frontend/src/components/hospital/HospitalGrid.tsx
    frontend/src/components/hospital/HospitalCard.tsx
    frontend/src/components/hospital/SearchInput.tsx
    frontend/src/components/hospital/Pagination.tsx
    frontend/src/components/dashboard/DashboardEmbed.tsx
    frontend/src/components/dashboard/DashboardErrorBoundary.tsx
    frontend/src/components/dashboard/FullscreenToggle.tsx
    frontend/src/app/[locale]/hospital/[cnes]/page.tsx
    frontend/src/messages/pt-BR/hospital.json
    frontend/src/messages/pt-BR/dashboard.json
  </read_first>
  <action>
Add `import { useTranslations } from 'next-intl'` (and `useLocale` where needed) to each component. Specific changes:

**HospitalGrid.tsx** -- namespace: `hospital`
- Add `const t = useTranslations('hospital')`
- Replace `"Nenhum hospital encontrado."` with `{t('noResults')}`
- Pass translated placeholder to SearchInput: `<SearchInput value={query} onChange={setQuery} placeholder={t('searchPlaceholder')} />`

**SearchInput.tsx** -- no useTranslations needed here
- The `placeholder` prop default value `'Buscar por nome ou CNES...'` should be removed. Change the default to just `placeholder = ''` since the parent (HospitalGrid) now passes the translated value. Or better: make placeholder required (remove the default).

**HospitalCard.tsx** -- namespace: `hospital`, plus locale-aware month formatting
- Must become a client component: add `'use client'` at top
- Add `import { useTranslations } from 'next-intl'` and `import { useLocale } from 'next-intl'`
- Add `const t = useTranslations('hospital')` and `const locale = useLocale()`
- Replace the `PT_MONTHS` array and `formatPeriod` function with `Intl.DateTimeFormat`:
  ```typescript
  function formatPeriod(dateStr: string | null, locale: string): string {
    if (!dateStr) return ''
    const d = new Date(dateStr + 'T00:00:00')
    const month = new Intl.DateTimeFormat(locale, { month: 'short' }).format(d)
    return `${month}/${d.getFullYear()}`
  }
  ```
  Delete the `PT_MONTHS` constant entirely.
  Call as: `formatPeriod(hospital.period_start, locale)`
- Replace `CNES {hospital.cnes}` with `{t('cnesLabel', { code: hospital.cnes })}`

**Pagination.tsx** -- namespace: `hospital`
- Must become a client component: add `'use client'` at top
- Add `import { useTranslations } from 'next-intl'`
- Add `const t = useTranslations('hospital')`
- Replace `aria-label="Paginacao"` with `aria-label={t('pagination.label')}`
- Replace `aria-label="Pagina anterior"` with `aria-label={t('pagination.previous')}`
- Replace `aria-label="Proxima pagina"` with `aria-label={t('pagination.next')}`

**DashboardEmbed.tsx** -- namespace: `dashboard`
- Add `const t = useTranslations('dashboard')`
- Replace `"Carregando dashboard..."` with `{t('loading')}`

**DashboardErrorBoundary.tsx** -- namespace: `dashboard`
- Add `const t = useTranslations('dashboard')`
- Replace `"Erro ao carregar dashboard"` with `{t('error.title')}`
- Replace `"O dashboard nao pode ser carregado. Verifique sua conexao e tente novamente."` with `{t('error.description')}`
- Replace `"Tentar novamente"` with `{t('error.retry')}`

**FullscreenToggle.tsx** -- namespace: `dashboard`
- Add `const t = useTranslations('dashboard')`
- Replace `aria-label={isFullscreen ? 'Sair da tela cheia' : 'Tela cheia'}` with `aria-label={isFullscreen ? t('fullscreen.exit') : t('fullscreen.enter')}`

**app/[locale]/hospital/[cnes]/page.tsx** (dashboard page) -- namespaces: `common`, `hospital`, `dashboard`
- Add `const tc = useTranslations('common')`, `const th = useTranslations('hospital')`, `const td = useTranslations('dashboard')`
- Replace `"Carregando..."` with `{tc('loading')}`
- Replace `"Hospital nao encontrado"` with `{th('notFound')}`
- Replace `"Voltar para a lista"` with `{th('backToList')}`
- Replace `"Voltar para hospitais"` with `{th('backToHospitals')}`
- Replace `"Dashboard nao configurado para este hospital."` with `{td('notConfigured')}`
  </action>
  <verify>
    <automated>cd frontend && grep -rn "Nenhum hospital\|Buscar por nome\|Paginacao\|Pagina anterior\|Proxima pagina\|Carregando dashboard\|Erro ao carregar\|Tentar novamente\|Tela cheia\|Sair da tela\|Hospital nao encontrado\|Voltar para\|Dashboard nao configurado\|PT_MONTHS\|CNES {" src/components/hospital/ src/components/dashboard/ src/app/\[locale\]/hospital/ 2>/dev/null | grep -v "node_modules" | grep -v ".json" | grep -v "useTranslations" | grep -v "import" || echo "PASS: No hardcoded Portuguese strings found in hospital/dashboard components"</automated>
  </verify>
  <done>
    - All 8 files import useTranslations from next-intl
    - HospitalGrid passes translated placeholder to SearchInput, uses t('noResults')
    - SearchInput no longer has hardcoded Portuguese default
    - HospitalCard uses Intl.DateTimeFormat(locale) for months, t('cnesLabel', { code })
    - PT_MONTHS array deleted from HospitalCard
    - Pagination uses t('pagination.label'), t('pagination.previous'), t('pagination.next')
    - DashboardEmbed uses t('loading')
    - DashboardErrorBoundary uses t('error.title'), t('error.description'), t('error.retry')
    - FullscreenToggle uses t('fullscreen.enter'), t('fullscreen.exit')
    - Dashboard [cnes] page uses tc('loading'), th('notFound'), th('backToList'), th('backToHospitals'), td('notConfigured')
    - Zero hardcoded Portuguese strings remain in any component
    - TypeScript compiles cleanly: `npx tsc --noEmit` passes
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run a comprehensive check:

1. `cd frontend && npx tsc --noEmit` -- zero TypeScript errors
2. Grep for remaining hardcoded Portuguese across ALL component files:
   ```bash
   grep -rn "Carregando\|Redirecionando\|Gestao Inteligente\|Entrar com\|Sair\b\|Mudar para\|Nenhum hospital\|Buscar por\|Paginacao\|Pagina anterior\|Proxima pagina\|Erro ao\|Tentar novamente\|Tela cheia\|Hospital nao\|Voltar para\|Dashboard nao\|Portal de Dashboard\|Popup bloqueado\|PT_MONTHS" frontend/src/components/ frontend/src/app/\[locale\]/ --include="*.tsx" | grep -v useTranslations | grep -v import | grep -v ".json"
   ```
   Should return zero results (only matches in .json files or import lines).
3. Confirm all components use useTranslations:
   ```bash
   grep -l "useTranslations" frontend/src/components/layout/AppHeader.tsx frontend/src/components/layout/AppFooter.tsx frontend/src/components/layout/UserMenu.tsx frontend/src/components/layout/ThemeToggle.tsx frontend/src/components/auth/LoginButton.tsx frontend/src/components/auth/AuthGuard.tsx frontend/src/components/hospital/HospitalGrid.tsx frontend/src/components/hospital/HospitalCard.tsx frontend/src/components/hospital/Pagination.tsx frontend/src/components/dashboard/DashboardEmbed.tsx frontend/src/components/dashboard/DashboardErrorBoundary.tsx frontend/src/components/dashboard/FullscreenToggle.tsx frontend/src/app/\[locale\]/page.tsx frontend/src/app/\[locale\]/hospital/\[cnes\]/page.tsx
   ```
   Should list all 14 files.
</verification>

<success_criteria>
- Every component with visible text uses useTranslations() from next-intl
- Zero hardcoded Portuguese strings in any .tsx file (excluding metadata in root layout.tsx)
- All t() key references match keys defined in the pt-BR JSON files
- HospitalCard uses Intl.DateTimeFormat for locale-aware month formatting (no PT_MONTHS array)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-i18n/04-02-SUMMARY.md`
</output>
