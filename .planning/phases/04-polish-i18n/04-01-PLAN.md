---
phase: 04-polish-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/next.config.ts
  - frontend/src/i18n/routing.ts
  - frontend/src/i18n/request.ts
  - frontend/src/proxy.ts
  - frontend/src/messages/pt-BR/common.json
  - frontend/src/messages/pt-BR/auth.json
  - frontend/src/messages/pt-BR/hospital.json
  - frontend/src/messages/pt-BR/dashboard.json
  - frontend/src/app/[locale]/layout.tsx
  - frontend/src/app/[locale]/page.tsx
  - frontend/src/app/[locale]/hospital/layout.tsx
  - frontend/src/app/[locale]/hospital/page.tsx
  - frontend/src/app/[locale]/hospital/[cnes]/page.tsx
  - frontend/src/app/layout.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-03

must_haves:
  truths:
    - "Navigating to /pt-BR/hospital shows the hospital grid (locale segment in URL)"
    - "Navigating to / redirects to /pt-BR (default locale applied by proxy)"
    - "API routes at /api/* still work without locale prefix"
    - "Adding a new locale requires only a new JSON file and a routing.ts entry"
  artifacts:
    - path: "frontend/src/i18n/routing.ts"
      provides: "Locale routing config"
      contains: "defineRouting"
    - path: "frontend/src/i18n/request.ts"
      provides: "Message loading config"
      contains: "getRequestConfig"
    - path: "frontend/src/proxy.ts"
      provides: "Locale detection and URL rewriting"
      contains: "export function proxy"
    - path: "frontend/src/app/[locale]/layout.tsx"
      provides: "NextIntlClientProvider wrapper"
      contains: "NextIntlClientProvider"
    - path: "frontend/src/messages/pt-BR/common.json"
      provides: "Shared UI strings"
      contains: "brand"
    - path: "frontend/src/messages/pt-BR/auth.json"
      provides: "Auth strings"
      contains: "loginButton"
    - path: "frontend/src/messages/pt-BR/hospital.json"
      provides: "Hospital strings"
      contains: "searchPlaceholder"
    - path: "frontend/src/messages/pt-BR/dashboard.json"
      provides: "Dashboard strings"
      contains: "loading"
  key_links:
    - from: "frontend/src/proxy.ts"
      to: "frontend/src/i18n/routing.ts"
      via: "import routing"
      pattern: "import.*routing.*from.*i18n/routing"
    - from: "frontend/src/app/[locale]/layout.tsx"
      to: "next-intl/server"
      via: "getMessages"
      pattern: "getMessages"
    - from: "frontend/next.config.ts"
      to: "next-intl/plugin"
      via: "createNextIntlPlugin"
      pattern: "createNextIntlPlugin"
---

<objective>
Install next-intl, configure [locale] routing infrastructure, create pt-BR message files, and restructure all page routes under app/[locale]/.

Purpose: Establishes the i18n architecture so components can use useTranslations() in Plan 02.
Output: Working locale routing with proxy.ts, message files, NextIntlClientProvider, all pages under [locale] segment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-i18n/04-CONTEXT.md
@.planning/phases/04-polish-i18n/04-RESEARCH.md

@frontend/src/app/layout.tsx
@frontend/src/app/page.tsx
@frontend/src/app/hospital/layout.tsx
@frontend/src/app/hospital/page.tsx
@frontend/src/app/hospital/[cnes]/page.tsx
@frontend/next.config.ts
@frontend/package.json

<interfaces>
<!-- Reference patterns from ferreiracontabilidade (D:\ferreiracontabilidade\app) -->

From D:\ferreiracontabilidade\app\src\i18n\routing.ts:
```typescript
import { defineRouting } from 'next-intl/routing'
export const routing = defineRouting({
  locales: ['pt-BR', 'en'],
  defaultLocale: 'pt-BR',
})
```

From D:\ferreiracontabilidade\app\src\i18n\request.ts:
```typescript
import { getRequestConfig } from 'next-intl/server'
import { hasLocale } from 'next-intl'
import { routing } from './routing'
const messageImports = {
  'pt-BR': async () => ({
    common: (await import('../messages/pt-BR/common.json')).default,
    auth: (await import('../messages/pt-BR/auth.json')).default,
  }),
} as const
export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale
  const locale = hasLocale(routing.locales, requested) ? requested : routing.defaultLocale
  const messages = await messageImports[locale]()
  return { locale, messages }
})
```

From D:\ferreiracontabilidade\app\src\proxy.ts:
```typescript
import createMiddleware from 'next-intl/middleware'
import { routing } from './i18n/routing'
const intlMiddleware = createMiddleware(routing)
export function proxy(request: NextRequest) {
  // ... auth checks ...
  return intlMiddleware(request)
}
export const config = {
  matcher: '/((?!api|_next|_vercel|.*\\..*).*)'
}
```

From D:\ferreiracontabilidade\app\src\app\[locale]\layout.tsx:
```typescript
import { NextIntlClientProvider } from 'next-intl'
import { getMessages } from 'next-intl/server'
export default async function LocaleLayout({ children, params }) {
  const { locale } = await params
  const messages = await getMessages()
  return (
    <NextIntlClientProvider locale={locale} messages={messages}>
      {children}
    </NextIntlClientProvider>
  )
}
```
</interfaces>

**CRITICAL Next.js 16 Notes:**
- Read `node_modules/next/dist/docs/` before writing code -- APIs may differ from training data
- `middleware.ts` is DEPRECATED. Use `proxy.ts` with `export function proxy()` instead.
- The next-intl `createMiddleware` still works, but wrap its return in the `proxy` export.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-intl and create i18n infrastructure</name>
  <files>
    frontend/package.json
    frontend/next.config.ts
    frontend/src/i18n/routing.ts
    frontend/src/i18n/request.ts
    frontend/src/proxy.ts
    frontend/src/messages/pt-BR/common.json
    frontend/src/messages/pt-BR/auth.json
    frontend/src/messages/pt-BR/hospital.json
    frontend/src/messages/pt-BR/dashboard.json
  </files>
  <read_first>
    frontend/AGENTS.md
    frontend/next.config.ts
    D:\ferreiracontabilidade\app\src\i18n\routing.ts
    D:\ferreiracontabilidade\app\src\i18n\request.ts
    D:\ferreiracontabilidade\app\src\proxy.ts
    D:\ferreiracontabilidade\app\next.config.ts
  </read_first>
  <action>
1. Install next-intl:
   ```bash
   cd frontend && npm install next-intl
   ```

2. Create `frontend/src/i18n/routing.ts` (per I18N-01, I18N-03):
   ```typescript
   import { defineRouting } from 'next-intl/routing'
   export const routing = defineRouting({
     locales: ['pt-BR'],
     defaultLocale: 'pt-BR',
   })
   ```
   Only pt-BR for now. Adding a locale later = add string here + new JSON folder.

3. Create `frontend/src/i18n/request.ts` with lazy message imports for all 4 namespaces (common, auth, hospital, dashboard):
   Follow the ferreiracontabilidade pattern exactly. The messageImports object must import from `../messages/pt-BR/common.json`, `../messages/pt-BR/auth.json`, `../messages/pt-BR/hospital.json`, `../messages/pt-BR/dashboard.json`.

4. Create `frontend/src/proxy.ts` -- CRITICAL: export must be named `proxy`, NOT `middleware`:
   ```typescript
   import createMiddleware from 'next-intl/middleware'
   import { routing } from './i18n/routing'
   import { NextRequest } from 'next/server'

   const intlMiddleware = createMiddleware(routing)

   export function proxy(request: NextRequest) {
     return intlMiddleware(request)
   }

   export const config = {
     matcher: '/((?!api|_next|_vercel|.*\\..*).*)'
   }
   ```
   No auth check in proxy -- GIS uses client-side AuthGuard, not middleware auth.

5. Update `frontend/next.config.ts` -- wrap with next-intl plugin:
   ```typescript
   import type { NextConfig } from 'next'
   import createNextIntlPlugin from 'next-intl/plugin'

   const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts')

   const nextConfig: NextConfig = {
     output: 'standalone',
     async headers() {
       return [{
         source: '/(.*)',
         headers: [
           { key: 'Cross-Origin-Policy', value: 'same-origin-allow-popups' },
           { key: 'Content-Security-Policy', value: "frame-src 'self' https://app.powerbi.com" },
         ]
       }]
     },
   }

   export default withNextIntl(nextConfig)
   ```
   Keep the existing headers() exactly as they are. Only add the createNextIntlPlugin wrapper.

6. Create all 4 message JSON files with the exact content from the RESEARCH.md "Recommended Message File Structure" section:

   `frontend/src/messages/pt-BR/common.json`:
   ```json
   {
     "brand": {
       "short": "G.I.S.",
       "full": "Gestao Inteligente em Saude",
       "tagline": "Portal de Dashboards Hospitalares"
     },
     "nav": {
       "avatar": "Avatar",
       "logout": "Sair"
     },
     "theme": {
       "switchToLight": "Mudar para tema claro",
       "switchToDark": "Mudar para tema escuro"
     },
     "footer": {
       "copyright": "G.I.S. Gestao Inteligente em Saude \u00a9 {year}"
     },
     "loading": "Carregando..."
   }
   ```

   `frontend/src/messages/pt-BR/auth.json`:
   ```json
   {
     "loginButton": "Entrar com Google",
     "redirecting": "Redirecionando...",
     "errors": {
       "googleNotLoaded": "Servico do Google nao carregado. Recarregue a pagina.",
       "generic": "Erro ao autenticar",
       "popupBlocked": "Popup bloqueado. Habilite popups para este site."
     }
   }
   ```

   `frontend/src/messages/pt-BR/hospital.json`:
   ```json
   {
     "searchPlaceholder": "Buscar por nome ou CNES...",
     "noResults": "Nenhum hospital encontrado.",
     "cnesLabel": "CNES {code}",
     "notFound": "Hospital nao encontrado",
     "backToList": "Voltar para a lista",
     "backToHospitals": "Voltar para hospitais",
     "pagination": {
       "label": "Paginacao",
       "previous": "Pagina anterior",
       "next": "Proxima pagina"
     }
   }
   ```

   `frontend/src/messages/pt-BR/dashboard.json`:
   ```json
   {
     "loading": "Carregando dashboard...",
     "notConfigured": "Dashboard nao configurado para este hospital.",
     "error": {
       "title": "Erro ao carregar dashboard",
       "description": "O dashboard nao pode ser carregado. Verifique sua conexao e tente novamente.",
       "retry": "Tentar novamente"
     },
     "fullscreen": {
       "enter": "Tela cheia",
       "exit": "Sair da tela cheia"
     }
   }
   ```
  </action>
  <verify>
    <automated>cd frontend && node -e "const r = require('./src/i18n/routing'); console.log(r)" 2>/dev/null || echo "Check TS compilation instead" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    - next-intl installed in package.json
    - routing.ts exports defineRouting with locales: ['pt-BR']
    - request.ts imports all 4 message namespaces (common, auth, hospital, dashboard)
    - proxy.ts exports function named `proxy` (NOT middleware)
    - next.config.ts wrapped with createNextIntlPlugin
    - All 4 message JSON files exist with complete pt-BR strings
  </done>
</task>

<task type="auto">
  <name>Task 2: Move page routes under [locale] segment</name>
  <files>
    frontend/src/app/layout.tsx
    frontend/src/app/[locale]/layout.tsx
    frontend/src/app/[locale]/page.tsx
    frontend/src/app/[locale]/hospital/layout.tsx
    frontend/src/app/[locale]/hospital/page.tsx
    frontend/src/app/[locale]/hospital/[cnes]/page.tsx
  </files>
  <read_first>
    frontend/src/app/layout.tsx
    frontend/src/app/page.tsx
    frontend/src/app/hospital/layout.tsx
    frontend/src/app/hospital/page.tsx
    frontend/src/app/hospital/[cnes]/page.tsx
    D:\ferreiracontabilidade\app\src\app\[locale]\layout.tsx
  </read_first>
  <action>
1. Create `frontend/src/app/[locale]/layout.tsx` -- the NextIntlClientProvider wrapper:
   ```typescript
   import { NextIntlClientProvider } from 'next-intl'
   import { getMessages } from 'next-intl/server'

   export default async function LocaleLayout({
     children,
     params,
   }: {
     children: React.ReactNode
     params: Promise<{ locale: string }>
   }) {
     const { locale } = await params
     const messages = await getMessages()
     return (
       <NextIntlClientProvider locale={locale} messages={messages}>
         {children}
       </NextIntlClientProvider>
     )
   }
   ```
   This is a SERVER component (no 'use client'). It wraps children with the message provider.

2. Move `frontend/src/app/page.tsx` to `frontend/src/app/[locale]/page.tsx`.
   Content stays identical -- it is already a 'use client' component. No string extraction in this plan (Plan 02 handles that).

3. Move `frontend/src/app/hospital/layout.tsx` to `frontend/src/app/[locale]/hospital/layout.tsx`.
   Content stays identical.

4. Move `frontend/src/app/hospital/page.tsx` to `frontend/src/app/[locale]/hospital/page.tsx`.
   Content stays identical.

5. Move `frontend/src/app/hospital/[cnes]/page.tsx` to `frontend/src/app/[locale]/hospital/[cnes]/page.tsx`.
   Content stays identical.

6. Delete the old route files:
   - Delete `frontend/src/app/page.tsx`
   - Delete `frontend/src/app/hospital/page.tsx`
   - Delete `frontend/src/app/hospital/[cnes]/page.tsx`
   - Delete `frontend/src/app/hospital/layout.tsx`
   - Remove `frontend/src/app/hospital/` directory if empty

7. Keep `frontend/src/app/layout.tsx` at root (do NOT move it). It stays as the root layout with html/body/ThemeProvider/AuthProvider/QueryProvider. It does NOT get NextIntlClientProvider -- that belongs in [locale]/layout.tsx only.

8. Keep `frontend/src/app/api/` at root (do NOT move under [locale]). The proxy.ts matcher already excludes /api paths.

**IMPORTANT:** Do NOT change any hardcoded strings in this task. The pages should work identically to before, just under the /pt-BR/ URL prefix. Plan 02 will extract strings.

**IMPORTANT:** All internal links (`/hospital`, `/hospital/{cnes}`, `/`) will continue to work because the proxy.ts detects bare paths and redirects to locale-prefixed versions. Do NOT modify Link href values or router.push paths in this task.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "---" && ls -la src/app/\[locale\]/page.tsx src/app/\[locale\]/hospital/page.tsx src/app/\[locale\]/hospital/\[cnes\]/page.tsx src/app/\[locale\]/layout.tsx 2>&1 && echo "---" && test ! -f src/app/page.tsx && echo "Old page.tsx removed" || echo "FAIL: old page.tsx still exists"</automated>
  </verify>
  <done>
    - app/[locale]/layout.tsx exists with NextIntlClientProvider
    - app/[locale]/page.tsx exists (login page, moved from app/page.tsx)
    - app/[locale]/hospital/layout.tsx exists (moved from app/hospital/layout.tsx)
    - app/[locale]/hospital/page.tsx exists (moved from app/hospital/page.tsx)
    - app/[locale]/hospital/[cnes]/page.tsx exists (moved from app/hospital/[cnes]/page.tsx)
    - Old route files deleted (app/page.tsx, app/hospital/ directory)
    - Root layout.tsx unchanged at app/layout.tsx
    - app/api/ unchanged at root level
    - TypeScript compiles with no errors
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd frontend && npx tsc --noEmit` passes with zero errors
2. `ls frontend/src/app/[locale]/page.tsx` exists
3. `ls frontend/src/proxy.ts` exists
4. `grep "createNextIntlPlugin" frontend/next.config.ts` finds the plugin wrapper
5. `grep "defineRouting" frontend/src/i18n/routing.ts` finds the routing config
6. `grep "export function proxy" frontend/src/proxy.ts` confirms correct export name
7. All 4 message JSON files exist under `frontend/src/messages/pt-BR/`
8. Old `frontend/src/app/page.tsx` no longer exists
</verification>

<success_criteria>
- next-intl is installed and configured with [locale] routing
- proxy.ts handles locale detection (export named `proxy`, not `middleware`)
- All page routes live under app/[locale]/
- API routes remain at app/api/ (not under [locale])
- 4 pt-BR message JSON files contain all UI strings
- Root layout.tsx is unchanged (html/body/providers)
- [locale]/layout.tsx wraps children with NextIntlClientProvider
- TypeScript compiles cleanly
- Architecture supports adding new locales by: (1) adding JSON files in messages/{locale}/, (2) adding locale to routing.ts locales array, (3) adding import entry in request.ts
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-i18n/04-01-SUMMARY.md`
</output>
