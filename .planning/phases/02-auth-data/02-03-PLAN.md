---
phase: 02-auth-data
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/app/hospital/page.tsx
  - frontend/src/app/hospital/[cnes]/page.tsx
  - frontend/src/components/auth/AuthGuard.tsx
autonomous: false
requirements: [SPA-03, SPA-04, SPA-05, AUTH-07]

must_haves:
  truths:
    - "Authenticated user navigating to / is shown the hospital grid page"
    - "Unauthenticated user navigating to /hospital or /hospital/[cnes] is redirected to /"
    - "URL /hospital/[cnes] shows a dashboard placeholder with the CNES in the URL"
    - "Browser back/forward moves between login, grid, and dashboard without full page reload"
    - "Client-side view transitions happen without full page reloads (SPA behavior)"
    - "Proxy route forwards auth-session cookie as Bearer header to Go API (already done in Phase 1)"
  artifacts:
    - path: "frontend/src/app/hospital/page.tsx"
      provides: "Hospital grid page (protected, fetches from /api/hospitals)"
      contains: "apiFetch.*hospitals"
    - path: "frontend/src/app/hospital/[cnes]/page.tsx"
      provides: "Dashboard page with CNES param (protected)"
      contains: "params.*cnes"
    - path: "frontend/src/components/auth/AuthGuard.tsx"
      provides: "Reusable auth protection wrapper"
      contains: "isAuthenticated"
    - path: "frontend/src/app/page.tsx"
      provides: "Root page: login if unauth, redirect to /hospital if auth"
      contains: "router.push.*hospital"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "/hospital"
      via: "useRouter().push on isAuthenticated"
      pattern: "router\\.push.*hospital"
    - from: "frontend/src/app/hospital/page.tsx"
      to: "/api/proxy/hospitals"
      via: "apiFetch"
      pattern: "apiFetch.*hospitals"
    - from: "frontend/src/components/auth/AuthGuard.tsx"
      to: "frontend/src/contexts/AuthContext.tsx"
      via: "useAuth()"
      pattern: "useAuth"
---

<objective>
SPA navigation: auth guard, hospital grid page, dashboard page, routing between views

Purpose: Connect the authentication layer (Plan 02) with actual page navigation. Authenticated users see the hospital grid fetched from the Go API (Plan 01). URL-based routing enables direct links to hospitals. Browser back/forward works natively via Next.js app router.

Output: AuthGuard component, hospital grid page at /hospital, dashboard page at /hospital/[cnes], and root page with auth-aware redirect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-data/02-CONTEXT.md
@.planning/phases/02-auth-data/02-RESEARCH.md

@frontend/src/contexts/AuthContext.tsx
@frontend/src/lib/api/client.ts
@frontend/src/app/api/proxy/[...path]/route.ts
@frontend/src/app/layout.tsx

<!-- Prior plan summaries needed for contracts -->
@.planning/phases/02-auth-data/02-01-SUMMARY.md
@.planning/phases/02-auth-data/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: Go API response contract -->
GET /api/hospitals (protected, requires Bearer token):
Response: Array of Hospital objects:
```json
[{
  "id": "uuid",
  "name": "Hospital Name",
  "cnes": "0000001",
  "logo_url": null,
  "period_start": "2024-01-01",
  "period_end": "2024-12-31",
  "powerbi_url": "https://app.powerbi.com/view?r=...",
  "sort_order": 1,
  "created_at": "2024-01-01T00:00:00Z"
}]
```

<!-- From Plan 02: AuthContext contract -->
From frontend/src/contexts/AuthContext.tsx:
```typescript
interface AuthUser {
  email: string; name: string; picture: string; sub: string;
}
function useAuth(): {
  user: AuthUser | null;
  isAuthenticated: boolean;
  isLoading: boolean;
  token: string | null;
  login: (credentialResponse: { credential?: string }) => Promise<void>;
  logout: () => Promise<void>;
}
```

From frontend/src/lib/api/client.ts:
```typescript
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<Result<T>>
// Routes through /api/proxy/* which adds Bearer token from auth-session cookie
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthGuard component, hospital grid page, dashboard page, and root page redirect</name>
  <files>
    frontend/src/components/auth/AuthGuard.tsx,
    frontend/src/app/hospital/page.tsx,
    frontend/src/app/hospital/[cnes]/page.tsx,
    frontend/src/app/page.tsx
  </files>
  <read_first>
    frontend/src/contexts/AuthContext.tsx,
    frontend/src/lib/api/client.ts,
    frontend/src/app/page.tsx,
    frontend/src/app/layout.tsx,
    frontend/next.config.ts,
    frontend/node_modules/next/dist/docs/ (check for routing docs, especially dynamic routes and useParams)
  </read_first>
  <action>
    IMPORTANT: Read relevant Next.js docs at frontend/node_modules/next/dist/docs/ BEFORE writing any page or routing code. Check for breaking changes in useRouter, useParams, Link, dynamic routes, and client components.

    1. Create frontend/src/components/auth/AuthGuard.tsx:
       - 'use client' directive
       - Import useAuth, useRouter, useEffect
       - Props: { children: ReactNode }
       - Logic:
         a. Get { isAuthenticated, isLoading } from useAuth()
         b. useEffect: if (!isLoading && !isAuthenticated) router.push('/')
         c. If isLoading: return centered spinner with "Carregando..."
         d. If !isAuthenticated: return null (redirect is happening)
         e. If isAuthenticated: return children

    2. Create frontend/src/app/hospital/page.tsx:
       - 'use client' directive
       - Wrap content in AuthGuard
       - Define Hospital type matching Go API response:
         ```typescript
         interface Hospital {
           id: string; name: string; cnes: string; logo_url: string | null;
           period_start: string | null; period_end: string | null;
           powerbi_url: string | null; sort_order: number; created_at: string;
         }
         ```
       - Use useEffect + useState to fetch hospitals:
         a. Call apiFetch<Hospital[]>('/hospitals')
         b. If ok, set hospitals state
         c. If error, show toast via sonner: toast.error(result.error.message)
       - Loading state: show "Carregando hospitais..." with spinner
       - Render hospital list as simple cards (Phase 3 will add full grid design):
         a. Map over hospitals
         b. Each card: Link to /hospital/{cnes}
         c. Show hospital name and CNES code
         d. Use basic Tailwind styling (grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4)
       - Header area with "Hospitais" title and logout button:
         a. Get { user, logout } from useAuth()
         b. Show user.name or user.email
         c. Logout button calls logout() then router.push('/')

       NOTE: This is a FUNCTIONAL grid, not the final designed grid (Phase 3 adds polish).
       The purpose here is to prove data flows from DB -> Go API -> proxy -> frontend.

    3. Create frontend/src/app/hospital/[cnes]/page.tsx:
       - 'use client' directive
       - Wrap content in AuthGuard
       - Get cnes from route params (use useParams or the page props pattern — check Next.js docs for current API)
       - Fetch hospital data: apiFetch<Hospital[]>('/hospitals') then find by cnes
         (In v1, we fetch all and filter client-side. Dedicated endpoint is Phase 3 scope.)
       - If hospital not found: show "Hospital nao encontrado" with back link
       - If hospital found and has powerbi_url:
         a. Show hospital name as title
         b. Show "Voltar" link to /hospital
         c. Show Power BI iframe placeholder div with message:
            "Dashboard Power BI sera exibido aqui" + the URL for reference
            (Phase 3 adds the actual iframe with CSP, loading indicator, fullscreen)
       - If hospital found but no powerbi_url: show "Dashboard nao configurado"

    4. Update frontend/src/app/page.tsx:
       - 'use client' directive
       - Import useAuth, useRouter, useEffect, LoginButton
       - Get { isAuthenticated, isLoading } from useAuth()
       - useEffect: if (!isLoading && isAuthenticated) router.push('/hospital')
       - If isLoading: centered spinner "Carregando..."
       - If isAuthenticated: show "Redirecionando..." (brief flash before router.push)
       - If !isAuthenticated: show login page (same as Plan 02 Task 2 output):
         a. Centered card with GIS branding
         b. LoginButton component
         c. palette.css themed

    5. Verify build and SPA navigation:
       ```bash
       cd frontend && npx next build
       ```
  </action>
  <verify>
    <automated>cd D:/gitlab/GIS/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "isAuthenticated" frontend/src/components/auth/AuthGuard.tsx
    - grep -q "router.push" frontend/src/components/auth/AuthGuard.tsx
    - grep -q "apiFetch.*hospitals" frontend/src/app/hospital/page.tsx
    - grep -q "AuthGuard" frontend/src/app/hospital/page.tsx
    - grep -q "Link.*hospital" frontend/src/app/hospital/page.tsx || grep -q "href.*hospital" frontend/src/app/hospital/page.tsx
    - grep -q "logout" frontend/src/app/hospital/page.tsx
    - test -f frontend/src/app/hospital/[cnes]/page.tsx
    - grep -q "cnes" frontend/src/app/hospital/[cnes]/page.tsx
    - grep -q "AuthGuard" frontend/src/app/hospital/[cnes]/page.tsx
    - grep -q "router.push.*hospital" frontend/src/app/page.tsx
    - grep -q "LoginButton" frontend/src/app/page.tsx
    - npx next build exits 0
  </acceptance_criteria>
  <done>SPA navigation complete: root page redirects authenticated users to /hospital, AuthGuard protects /hospital and /hospital/[cnes], hospital grid fetches data from Go API via proxy, dashboard page displays hospital info by CNES from URL. All navigation is client-side (no full page reloads). Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full auth flow and SPA navigation end-to-end</name>
  <files>none</files>
  <action>
    Human verification of the complete Phase 2 deliverable. No code changes — this is a manual testing checkpoint.

    Prerequisites for user:
    1. Create a `.env` file with real GOOGLE_CLIENT_ID and your email in ALLOWED_EMAILS
    2. Start Docker Desktop
    3. Run: `docker compose up --build -d`
    4. Wait for all services healthy: `docker compose ps`
  </action>
  <what-built>
    Complete authentication and data flow:
    - Google OAuth popup login with server-side verification
    - Email whitelist enforcement
    - Session persistence via httpOnly cookie
    - Hospital grid loaded from database via Go API
    - SPA navigation between login, grid, and dashboard views
    - Browser back/forward navigation
  </what-built>
  <how-to-verify>
    Test steps:
    1. Open http://localhost:3000 — should see GIS login page with "Entrar com Google" button
    2. Click "Entrar com Google" — Google OAuth popup should appear
    3. Sign in with a whitelisted Google account — should redirect to /hospital showing hospital cards
    4. Try with a non-whitelisted account — should see "Email nao autorizado" error
    5. On /hospital page, verify all 12 hospitals appear with names and CNES codes
    6. Click any hospital card — should navigate to /hospital/{cnes} showing hospital info
    7. Click "Voltar" — should return to /hospital grid
    8. Use browser back/forward buttons — should navigate correctly without full page reloads
    9. Close browser tab, reopen http://localhost:3000 — should be already authenticated (session persists)
    10. Click logout — should return to login page, refreshing should show login (session cleared)

    Expected behavior:
    - No full page reloads during navigation (check network tab — no document requests after initial load)
    - Loading spinners shown during auth check and data fetch
    - Portuguese error messages for auth failures
  </how-to-verify>
  <verify>Human confirms all 10 test steps pass</verify>
  <done>Full Phase 2 auth + data + SPA navigation verified working end-to-end in Docker Compose</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` succeeds
- AuthGuard protects /hospital and /hospital/[cnes] routes
- Root page redirects authenticated users to /hospital
- Hospital grid page fetches from Go API via proxy
- Dashboard page shows hospital data by CNES from URL
- Browser back/forward works without full page reloads
- Full auth flow works end-to-end with Docker Compose
</verification>

<success_criteria>
- SPA navigation between 3 views: login (/), grid (/hospital), dashboard (/hospital/[cnes])
- No full page reloads on navigation (Next.js client-side routing)
- AuthGuard redirects unauthenticated users to login
- Hospital data fetched from Go API through proxy with auth headers
- Browser back/forward works correctly
- Session survives tab close (cookie persistence)
- Human verification confirms full flow works
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-data/02-03-SUMMARY.md`
</output>
