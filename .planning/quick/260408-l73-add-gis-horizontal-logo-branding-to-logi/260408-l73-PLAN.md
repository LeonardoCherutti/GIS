---
type: quick
task_id: 260408-l73
description: Add GIS horizontal logo branding to login page and dashboard header
files_modified:
  - frontend/src/app/[locale]/page.tsx
  - frontend/src/components/layout/AppHeader.tsx
---

<objective>
Replace text-only branding with the GIS horizontal logo image on both the login page and the dashboard header. The horizontal logo (`logo_horizontal.png`, 1941x658px) contains the heart-brain icon, "G.S.I" text, full name, and accent bars — so all redundant text elements should be removed.

Purpose: Professional branded appearance instead of plain text placeholders.
Output: Updated login page and header component with logo images.
</objective>

<context>
@frontend/src/app/[locale]/page.tsx
@frontend/src/components/layout/AppHeader.tsx
@frontend/src/components/hospital/HospitalCard.tsx (next/image usage pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add horizontal logo to login page</name>
  <files>frontend/src/app/[locale]/page.tsx</files>
  <action>
    1. Add `import Image from 'next/image'` at the top.
    2. Widen the card container from `max-w-sm` to `max-w-md` to give the horizontal logo breathing room.
    3. Replace the entire branding div (the `div.text-center.mb-8` containing h1 "brand.short", p "brand.full", p "brand.tagline") with a single centered `Image` component:
       ```
       <div className="flex justify-center mb-8">
         <Image
           src="/logo_horizontal.png"
           alt="G.S.I - Gestao Inteligente em Saude"
           width={388}
           height={132}
           priority
           className="dark:brightness-200 dark:contrast-50"
         />
       </div>
       ```
       - width/height are 1941/658 scaled down to ~388x132 (1/5 ratio), fitting nicely in the md card.
       - `priority` since it's above the fold on the login page.
       - `dark:brightness-200 dark:contrast-50` makes the dark blue/pink logo text visible on dark backgrounds by boosting brightness and reducing contrast. Test visually — if it looks off, try `dark:invert` instead.
    4. Keep the LoginButton component exactly as-is below the logo.
    5. The `useTranslations('common')` import (`tc`) is still needed for the loading state text. The `useTranslations('auth')` import (`ta`) is still needed for the redirecting text. Keep both.
  </action>
  <verify>
    Run `cd D:/gitlab/GIS/frontend && npx next build 2>&1 | tail -5` — build succeeds with no errors.
    Visually: login page shows horizontal logo centered in card, no duplicate text branding.
  </verify>
  <done>Login page displays horizontal logo image instead of text-only branding. Card is max-w-md. Dark mode has filter applied for visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Add logo to dashboard header</name>
  <files>frontend/src/components/layout/AppHeader.tsx</files>
  <action>
    1. Add `import Image from 'next/image'` at the top.
    2. Remove the `import { Building2 } from 'lucide-react'` import (no longer needed).
    3. Replace the left-side content (the Building2 icon + div with brand.short/brand.full spans) with a single Image component:
       ```
       <Image
         src="/logo_horizontal.png"
         alt="G.S.I"
         width={156}
         height={53}
         className="dark:brightness-200 dark:contrast-50"
       />
       ```
       - Sized at ~156x53 (1/12.4 ratio of 1941x658) which gives roughly 40px visual height with some padding — appropriate for a nav bar.
       - Same dark mode filter as login page for consistency.
    4. Remove the `useTranslations` import and the `t` const if no other usage remains in the component. Check: `t('brand.short')` and `t('brand.full')` were the only usages. If so, remove both the import and the const.
    5. Keep ThemeToggle and UserMenu on the right side exactly as-is.
    6. The outer header element and right-side div remain unchanged.
  </action>
  <verify>
    Run `cd D:/gitlab/GIS/frontend && npx next build 2>&1 | tail -5` — build succeeds with no errors.
    Visually: header shows horizontal logo at nav-bar size, no Building2 icon or text branding.
  </verify>
  <done>Dashboard header displays horizontal logo image replacing the Building2 icon + text. Dark mode filter applied. No unused imports remain.</done>
</task>

</tasks>

<verification>
- `cd D:/gitlab/GIS/frontend && npx next build` completes without errors
- Login page: horizontal logo centered in card, no redundant text, card is max-w-md
- Dashboard header: horizontal logo at ~40px height, clean proportions
- Dark mode: logo remains visible with brightness/contrast filter
</verification>

<success_criteria>
Both the login page and dashboard header show the GIS horizontal logo image instead of text-only branding. Build passes. No unused imports.
</success_criteria>
